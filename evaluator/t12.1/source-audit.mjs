import assert from 'node:assert/strict';import fs from 'node:fs';import path from 'node:path';
const root=path.resolve(import.meta.dirname,'../..'),p=path.join(root,'scripts/verify-performance-baseline.mjs'),required=process.env.T121_REQUIRE_PRODUCTION==='1';if(!fs.existsSync(p)){if(required)throw Error('T12.1 production driver missing');process.stdout.write('PASS T12.1-SOURCE-AUDIT (production intentionally absent during evaluator authoring)\n');process.exit(0)}const s=fs.readFileSync(p,'utf8');
for(const [name,re] of [['fixed frames',/\b300\b/],['warm frames',/\b30\b/],['plans',/ALLSTATS LAST/i],['cursor source',/V\$SQL/i],['external clock',/(performance\.now|hrtime)/],['atomic output',/(rename|atomic)/i],['redaction',/(redact|secret)/i]])assert.match(s,re,name);assert.doesNotMatch(s,/(dry.?run|mock|synthetic.?pass|skip.?database)/i);process.stdout.write('PASS T12.1-SOURCE-AUDIT (baseline driver contract present)\n');
