diff --git a/src/mochadoom/Engine.java b/src/mochadoom/Engine.java
--- a/src/mochadoom/Engine.java
+++ b/src/mochadoom/Engine.java
@@ -32,5 +32,6 @@ import java.util.Arrays;
 import java.util.logging.Level;
 import java.util.logging.Logger;
+import utils.ResourceIO;
 
 public class Engine {
     private static volatile Engine instance;
@@ -66,6 +67,7 @@ public class Engine {
     /** Construct the engine without a desktop window or free-running loop. */
     public static DoomMain<?, ?> createHeadless(final String... argv) throws IOException {
         synchronized (Engine.class) {
+            ResourceIO.disableExternalIO();
             final Engine local = new Engine(true, argv);
             return local.DOOM;
         }
diff --git a/src/utils/ResourceIO.java b/src/utils/ResourceIO.java
--- a/src/utils/ResourceIO.java
+++ b/src/utils/ResourceIO.java
@@ -36,6 +36,16 @@ public class ResourceIO {
     private final Path file;
     private final Charset charset = Charset.forName("US-ASCII");
 
+    private static volatile boolean externalIODisabled;
+
+    public static void disableExternalIO() {
+        externalIODisabled = true;
+    }
+
+    public static boolean isExternalIODisabled() {
+        return externalIODisabled;
+    }
+
     public ResourceIO(final File file) {
         this.file = file.toPath();
     }
@@ -50,10 +56,12 @@ public class ResourceIO {
     }
 
     public boolean exists() {
+        if (externalIODisabled) return false;
         return Files.exists(file);
     }
 
     public boolean readLines(final Consumer<String> lineConsumer) {
+        if (externalIODisabled) return false;
         if (Files.exists(file)) {
             try (BufferedReader reader = Files.newBufferedReader(file, charset)) {
                 String line;
@@ -73,6 +81,7 @@ public class ResourceIO {
     }
 
     public boolean writeLines(final Supplier<String> lineSupplier, final OpenOption... options) {
+        if (externalIODisabled) return false;
         try (BufferedWriter writer = Files.newBufferedWriter(file, charset, options)) {
             String line;
             while ((line = lineSupplier.get()) != null) {
