# T11.2 moving-input scoring incident — 2026-07-26

Classification: `VOIDED_HARNESS_DEFECT`.

The first post-static-handler rerun failed the unchanged unique-moving-frame
assertion with 299 unique frames in a 300-frame, sequential-tic sample. The
failed browser ledger was retained by the fail-closed driver before cleanup.

- Scored tics: 223–522
- Duplicate: tic 224 (same pixels as tic 223)
- First forward input effective tic: 226
- First combined forward-and-turn effective tic: 228
- Observed cadence: 34.476 FPS, 32.7 ms p95
- Browser/network errors: zero
- Capacity release: PASS

Root cause: the Playwright scorer waited for the presence of any historical
`input-effective` diagnostic. A neutral one-second heartbeat already satisfied
that predicate, so the 32-tic settling window was measured from neutral tic
189 rather than from the authored moving command. The scorer therefore began
at tic 223, before movement became authoritative, and its all-unique assertion
correctly rejected a repeated neutral view.

Correction: both the wait predicate and effective-tic extraction now require
the same non-zero forward-and-turn command. Two
`T112_MOVING_INPUT_EFFECTIVE_FENCE` source markers make the ordering
fail-closed. The all-unique, sequential-tic, 30 FPS, and 33.333 ms p95
acceptance rules were not changed.

The clean rerun passed with 300/300 unique frames at 34.152 FPS and 32.6 ms
p95. Its complete sanitized evidence is
`t11.2-oci-hosted-evidence-2026-07-26.json` with file SHA-256
`88d23126488dc67b7b14a12718d23605f0ac1f810825853ee574ae1f2a166f0e`.
