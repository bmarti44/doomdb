# Async-admission lifecycle race battery — 2026-07-25

Final authority artifact:
`5ec18cbe4cff7192d384e81d1010e0133d357d44ff17fa65821e1489c4fd1ee3`.
Canonical table pack:
`058cd0df9444131b356762a096fd422d5131ac3aea91163aee056e8ad4965b44`.

The complete four-cell battery passed under the recorded Free-edition
Resource Manager envelope. Its alert-log postflight found exactly the one
expected `ORA-00031` generated by the deliberate dispatcher-session kill
and no unclassified Oracle incidents.

| Cell | Failure class | Fix exercised | Terminal result |
|:--|:--|:--|:--|
| READY/status claim storm | Duplicate asynchronous admission | Generation-fenced, idempotent authority claim | 8 concurrent callers, exactly 1 authority claim |
| Member LEAVE during STARTING | Nonviable row retaining capacity | Atomic terminal cancellation and capacity release | `CANCELLED`, active capacity 0 |
| Worker FAILED during STARTING | Client stuck in STARTING | Bounded failed-control reconciliation plus standby stop intent | Client-visible `CANCELLED` within 5,000 ms |
| Dispatcher death before initial publish | Stale assignment blocks re-claim | Full-incarnation liveness check, fail stale assignment/slot, retry one replacement claim | 1 stale claim failed, exactly 1 replacement live |

The production two-slot retained pool returned to two idle `READY` slots
after the destructive cell.

Primary evidence:
`async-admission-races-decps-5ec18cbe-2026-07-25-v5.log`.

The `v4` artifact is `VOIDED`: its aggregate run overlapped the focused
dispatch-death cell's production-pool rebuild. It is retained without
alteration as diagnostic evidence and is not cited as a gate result.
