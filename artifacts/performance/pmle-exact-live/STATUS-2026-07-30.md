# Exact live database renderer status — 2026-07-30

## Movement-stall and combined-locator result

The public OCI Always Free deployment is currently exercising the proven
high-fidelity Mocha renderer with a candidate DPV2 publication coordinator:

- renderer SHA-256:
  `51aadc21bcb619928cee3e73217c8150cebf19aa3186dfd273ea3535d88e2edb`
- deployed native-temporal coordinator SHA-256:
  `55d86b81e6e76ee4416622a59052526481ff2d14536b68f2535ca291b246d85b`

DPV2 changes persistence only. Three consecutive, complete 320x200 temporal
frames and both authorized POVs cross one persistent locator. The
definer-rights transport validates the full bundle and extracts only the
requester's authenticated viewpoint into the existing DPB2 wire shape.
The browser still receives database-generated pixels; simulation,
rasterization, palettes, input ordering, and canvas code are unchanged.

The local gate proves every pixel of every reconstructed tic, distinct
two-player POVs, forward displacement, turning, generation reset, and one
locator per temporal bundle. The database fixture proves two-player
authentication, suffix extraction, and fail-closed malformed input.

The live normal-path movement gate passed:

- 34.084 FPS over 20 seconds
- 691 canvas presentations
- 689 unique database and canvas frames
- 688 consecutive framebuffer changes
- changed-pixel p50/p95: 4,873 / 15,671
- 62 of 63 held-forward inputs effective
- exchange p50/p95: 42.019 / 52.349 ms
- zero cancelled requests; two teardown-time failed requests

This refutes the repeated-frame hypothesis for the measured route and removes
the reported 10–30 second movement freeze on the normal public path. Under an
additional synthetic 200 ms RTT, authoritative movement still passed and the
maximum paint gap was 159.4 ms, but throughput fell to 15.709 FPS. That cell
is diagnostic and fails the 30 FPS gate; the single-flight network leg, not
stale rendering, is then the limit.

The first two-browser DPV2 diagnostic delivered 300 unique consecutive frames
to each POV at 20.274 and 20.159 average FPS. It is not an accepted multiplayer
gate: gap p95 remained 143.1/138.6 ms and the harness terminated on insufficient
input-overlay sampling. DPV2 therefore remains a live candidate while the
20-FPS multiplayer playout policy is completed; solo is already above its
30-FPS gate.

## Proven high-fidelity renderer lineage

The public deployment continues to use the proven high-fidelity Mocha
renderer. Its pre-temporal public moving/firing measurement was 19.642 FPS in
`bsp-precomputed-geometry-public-fps-2026-07-30.log`.

## BSP event-stream experiment

The flattened BSP event stream preserved exact frames and canonical state
over 1,500 tics and passed bidirectional cross-artifact recovery. Its paired
stage cell improved BSP mean by 16.917 percent and total render mean by
12.922 percent. It nevertheless regressed the retained public path to
11.721 FPS. It was rolled back rather than promoted. The experiment also
revealed that its event-only fields had contaminated the nominal control
bridge shape; BSP-event sources are now isolated in their own overlay.

A corrected successor reuses the exact unsigned endpoint angle already
computed by the event kernel instead of repeating `PointToAngle` through the
generated Java path for every visible wall. It again passed the 1,500-tic
frame/state/recovery differential. On OCI its paired means changed from
29.454 to 21.344 ms for BSP and from 45.343 to 37.150 ms total. Dense
windows improved from 76.593/70.245 to 57.272/56.504 ms. The retained public
moving/firing result improved from 17.683 to 19.026 FPS, so this corrected
artifact was promoted while the larger fused-wall work continues.

The next successor also computes the exact normal angle, distance,
hypotenuse, endpoint scales, and scale step inside that same typed traversal.
It avoids re-entering the generated Java geometry path in `StoreWallRange`
while leaving the original Mocha wall setup, clipping, rasterization, plane,
drawseg, and masked-sprite side effects intact.

Correctness passed over 1,500 tics with zero frame mismatches, identical
frame/state chains, and all own-artifact and cross-artifact checkpoint
restores. Against the angle-reuse artifact on OCI, its paired means changed
from 21.344 to 18.245 ms for BSP and from 37.150 to 33.799 ms total. Dense
windows improved from 57.272/56.504 to 50.812/49.931 ms. The production
artifact then passed the same 1,500-tic differential and improved the public
moving/firing result from 19.026 to 19.642 FPS. It is the deployed renderer
while fused-wall work continues.

## Store-wall geometry experiment

`0031-teavm-store-wall-geometry-jsbody.patch` moves only the pure fixed-point
wall geometry block into one typed-array JavaScript crossing. Mocha continues
to own drawseg, visplane, texture, masked-sprite, and clipping side effects.

Correctness:

- 300-tic exact-frame and canonical-state differential: PASS
- 1,500-tic exact-frame and canonical-state differential: PASS
- own-artifact and bidirectional cross-artifact recovery: PASS
- raw checkpoint difference is restricted to the already-understood
  canonical save-pointer word (`host identity` versus stable `1`)

Paired OCI 1,500-tic result:

| Metric | Control mean | Candidate mean | Change |
|---|---:|---:|---:|
| BSP | 28.929 ms | 27.731 ms | -4.141% |
| Planes | 9.056 ms | 9.301 ms | +2.701% |
| Masked | 6.329 ms | 6.576 ms | +3.905% |
| Total | 44.465 ms | 43.773 ms | -1.556% |

Twelve of fifteen total-frame windows improved, but the total gain is below
the 5 percent promotion threshold. The candidate is therefore rejected.

## Original Doom Low Detail experiment

The engine's authentic Low Detail mode was measured on the same OCI venue and
same 1,500-tic combat stream. It retains the 320x200 frame, full HUD, weapons,
sprites, menus, lighting, and game rules while horizontally doubling the
world view as the original executable did.

It is useful but insufficient: dense windows remain 53.595–63.406 ms
(15.8–18.7 FPS), while quiet windows reach 20.342–26.388 ms
(37.9–49.2 FPS). It is not promoted as the 30 FPS solution.

## Store-wall source-split experiment

`0032-teavm-store-wall-range-source-split.patch` preserved the original
Mocha operations but emitted wall preparation and finalization as separate
compiler units. In a debug-named census, `StoreWallRange` fell from 14,429
to 10,035 generated bytes; the new helpers were 3,196 and 1,802 bytes.

Correctness passed over 1,500 tics with zero frame mismatches, identical
frame/state chains, and all own-artifact and cross-artifact checkpoint
restores. The paired OCI result was nevertheless performance-neutral:

| Metric | Control mean | Candidate mean | Change |
|---|---:|---:|---:|
| BSP | 29.454 ms | 29.625 ms | +0.578% |
| Planes | 9.337 ms | 9.155 ms | -1.954% |
| Masked | 6.395 ms | 6.429 ms | +0.532% |
| Total | 45.343 ms | 45.375 ms | +0.070% |

Only three of fifteen total-frame windows improved. This rejects the
compiler-digestibility hypothesis for this method split: the next candidate
must eliminate or batch substantial wall setup work rather than only
repartitioning the generated source.

## Current performance constraint

The deployed precomputed-geometry renderer averages:

- BSP: 18.245 ms
- planes: 9.059 ms
- masked sprites: 6.351 ms
- exact render total: 33.799 ms

Quiet windows already clear 30 FPS. The two densest windows remain
50.812/49.931 ms, so the remaining blocker is peak wall/plane/masked work,
not egress or canvas drawing. Public end-to-end moving/firing delivery is
19.642 FPS. The next candidate must fuse substantial `StoreWallRange`
setup, clipping, rasterization, and plane work; small scalar substitutions
are below the required leverage.

## Wall-path diagnostic decomposition

Two diagnostic-only OCI cells used the same 1,500-tic stream and build
lineage as the clean control:

| Cell | BSP mean | Planes mean | Masked mean | Total mean |
|---|---:|---:|---:|---:|
| Clean control | 29.454 ms | 9.337 ms | 6.395 ms | 45.343 ms |
| Wall pixel/clip loop suppressed | 19.592 ms | 2.399 ms | 6.456 ms | 28.595 ms |
| Entire `StoreWallRange` suppressed | 12.176 ms | 0.049 ms | 5.257 ms | 17.623 ms |

Dense windows 101–200 and 201–300 were respectively:

- clean: 76.593 / 70.245 ms
- pixel/clip loop suppressed: 54.875 / 52.605 ms
- all `StoreWallRange` work suppressed: 32.562 / 31.867 ms

These are diagnostic floors, not valid frames: suppressing the wall loop also
changes clip/plane population and suppressing `StoreWallRange` removes all
wall/drawseg side effects. They nevertheless close two narrower paths:

1. Pixel-loop optimization alone cannot reach 30 FPS; dense frames remain
   above 52 ms without any wall pixels.
2. Wall setup alone is also too large. Reaching 30 FPS requires a fused
   typed-array path covering traversal-visible wall setup, wall pixels,
   clipping, and plane population, followed by additional masked-stage work.

The combined structural-ceiling cell replaced legacy BSP traversal/clipping
with the exact typed-array event kernel while suppressing `StoreWallRange`.
It reduced the diagnostic total from 17.623 to 10.649 ms average. The two
dense windows fell from 32.562/31.867 to 19.086/19.216 ms. This leaves a
measured dense-scene budget of about 14 ms for a fused typed-array wall setup,
wall raster, clipping, and plane-population implementation. Unlike the
narrower pixel-only paths, that architecture has sufficient arithmetic
headroom to pursue the 30 FPS gate.

## Native wall-clip copy experiment

`0034-teavm-native-wall-clip-copy.patch` replaced the two non-overlapping
wall clip `System.arraycopy` operations with direct `Int16Array.subarray/set`.
It passed the 1,500-tic frame/state/recovery differential, but regressed the
OCI mean from 33.799 to 34.864 ms (+3.15 percent). Dense windows changed from
50.812/49.931 to 52.691/51.590 ms. TeaVM's existing arraycopy is therefore
already cheaper than an additional JS crossing at this cardinality. The
candidate is rejected and is not part of the deployed renderer.

## Precomputed wall-setup experiments

A full scalar-setup candidate moved sector comparisons, pegging, texture
selection, lighting, edge fractions, silhouette selection, and clip flags
into the typed BSP event kernel. It remained exact over 1,500 tics, but
regressed the OCI mean from 33.799 to 34.966 ms; dense windows changed from
50.812/49.931 to 51.700/51.367 ms.

A narrower successor specialized only one-sided walls and removed the
per-frame texture metadata scan. It also remained exact, but measured
34.503 ms overall with dense windows of 52.675/51.401 ms. Both candidates
are rejected. Relocating scalar setup without eliminating the per-wall
`StoreWallRange` object/control boundary does not provide leverage.

## Pre-lit wall-atlas experiment

The current-lineage Node CPU profile identified the two-stage texture then
colormap gather as the largest individual wall-pixel hotspot. An exact
candidate lazily materialized texture/light-table atlases so each rendered
pixel required one indexed read. The 1,500-tic differential passed, but the
OCI cell produced 100–1,696 ms first-use stalls, exhausted its 256-entry
combination cache near tic 1,100, and raised ORA-04161. A fully warm dense
window was still 50.095 ms versus the 49.931 ms control. The working-set and
combination cardinality erase the saved gather, so this path is rejected.

## Frame-level wall-column command batch

`0037-teavm-frame-wall-column-batch.patch` kept the existing exact wall
clipping, plane marking, texture selection, lighting, and command order, but
deferred the selected wall-column pixel commands into a reusable typed-array
queue and flushed them through one loop per frame. The candidate passed the
1,500-tic frame/state/checkpoint differential with zero mismatches.

The first OCI invocation accidentally exercised the constant-input fallback
because the SQL wrapper did not propagate the client identifier; it is
classified as diagnostic-only and is not used in the verdict. The corrected
`LIVE_DM` run measured 34.462 ms overall. Its two densest windows measured
51.211/50.652 ms, versus 50.812/49.931 ms for the deployed geometry artifact.
The candidate is therefore rejected and was not deployed. Removing repeated
column-loop construction and consolidating pixel execution does not offset
the queue/reference bookkeeping; the remaining dense-scene cost is the
pixel work itself plus plane and masked rendering.

## Frame-level plane-span command batch

`0038-teavm-frame-plane-span-batch.patch` deferred exact plane spans into a
reusable command buffer and flushed them once per frame. It passed the
1,500-tic frame/state/checkpoint differential, but increased the dense plane
windows from 10.954/10.280 ms to 12.054/11.491 ms and the dense total from
50.812/49.931 ms to 52.228/51.371 ms. Per-span Java registry and command
writes cost more than the removed wrapper construction. The candidate is
rejected.

## Masked/player typed-loop experiments

The first masked/player candidate (`0039`) replaced the generated
`R_DrawColumnBoom.Indexed` implementation with a separate typed JavaScript
bridge class. It remained exact, and its isolated masked stage improved by
roughly 0.5–0.6 ms in both dense windows. However, the extra generated class
changed surrounding generated-code layout enough to regress BSP and the
total dense frame. It was not promoted.

`0040-teavm-inplace-masked-player-column.patch` instead invokes the same exact
typed loop from inside the existing `R_DrawColumnBoom.Indexed` class, leaving
the original Java loop as its fail-safe fallback. This preserves the
high-fidelity renderer and avoids adding a replacement renderer class.

The candidate passed a 1,500-tic differential with:

- zero frame mismatches;
- identical frame and canonical-state chains;
- three raw checkpoint byte matches;
- three own-artifact and three cross-artifact restore comparisons.

The clean `LIVE_DM` OCI result is the first improvement in this experiment
series:

| Metric | Deployed geometry | In-place masked | Change |
|---|---:|---:|---:|
| all-window total mean | 33.799 ms | 32.583 ms | -3.60% |
| dense total 101–200 | 50.812 ms | 48.767 ms | -4.03% |
| dense total 201–300 | 49.931 ms | 48.249 ms | -3.37% |
| dense masked 101–200 | 10.878 ms | 9.910 ms | -8.90% |
| dense masked 201–300 | 11.550 ms | 10.473 ms | -9.32% |

This is an accepted optimization building block, not a 30 FPS verdict. It
raises worst-window throughput to about 20.5–20.7 FPS; BSP plus planes still
consume 38.5–38.7 ms in those windows. The production pool was restored to
two READY slots after the diagnostic, and all diagnostic database objects
were removed. Deployment remains deliberately pending a combined candidate
with enough additional BSP/plane improvement to justify an artifact
promotion and its full release battery.

### Production-entry promotion attempt

The incremental-deployment policy was exercised against the accepted masked
loop rather than waiting for a 30 FPS aggregate candidate.

The original `9752e327…` artifact could not be promoted: it selects the
stage-telemetry main class and intentionally omits authority/checkpoint
exports required by the retained coordinator. The production warmup caught
this fail-closed (`authorityEngine.canonicalState is not a function`); no
match was admitted and the prior renderer was restored with two READY slots.

A normal-entry production build, `ba71145d…` (1,719,428 bytes), passed the
full 1,500-tic pixel/state/checkpoint/cross-restore differential. Its public
moving/firing measurement was nevertheless only 12.391 FPS, versus 19.642
FPS for the prior deployment, so it was rejected and rolled back. A fresh
post-rollback public measurement of the known-good `51aadc21…` renderer
reported 20.459 FPS.

This establishes a new promotion rule: stage-telemetry speed is diagnostic
only. Every incremental candidate must expose the production coordinator
contract, run through retained-slot warmup, and beat the deployed artifact in
the public unique-moving-frame test. Exact pixels alone do not imply equal
speed because TeaVM whole-module entry point and generated layout materially
affect compilation on the managed OCI venue.

A follow-up removed the retained Java fallback entirely and placed the same
exact typed loop inside the existing renderer class, avoiding the added bridge
class as an explanation. Artifact `0b710bf7…` again passed the complete
1,500-tic differential, but produced only 12.466 public FPS. It too was
rejected and rolled back. This confirms that the regression is generated
production-shape/compiler behavior, not execution of the fallback or a
separate bridge-class dispatch. The deployed `51aadc21…` source and two READY
slots were re-verified after rollback.

A content-addressed post-link experiment then replaced only the uniquely
identified `R_DrawColumnBoom.Indexed.invoke` function inside the deployed
artifact. The patcher fences the input SHA, exact definition boundary, class
registration, and removed exception path. It reduced the module by 1,081
bytes and passed the same 1,500-tic differential. Unlike either global
rebuild, it preserved the favorable production compilation tier, but its
public measurements were 17.565 FPS (including one 5.7-second lifecycle
stall) and 19.103 FPS on a warm repeat. That does not beat the 20.459 FPS
fresh deployed baseline, so the candidate was rejected and rolled back.

Post-link replacement is retained as a valid shape-preserving experiment
mechanism, but the masked-column loop alone lacks enough leverage. The next
use must target a larger ranked hot function and must still pass the public
promotion gate.

The post-link loop was then corrected to preserve TeaVM's existing four-pixel
unrolling while removing only the never-taken Java exception/formatter tail.
Artifact `34c5026e…` was 876 bytes smaller than production and passed all
1,500 exactness/recovery checks. Public measurements were 19.433 FPS (with a
2.86-second lifecycle stall) and 19.845 FPS warm; median gaps were
42.5–43.4 ms. Although the normal-frame median improved slightly, aggregate
unique-frame throughput did not beat the predeclared 20.459 FPS control, so
it was not promoted. Production was again restored and verified at
`51aadc21…`, two READY slots, zero FAILED slots.

## In-place visplane experiment

`0041-teavm-inplace-flat-span.patch` applied the successful masked-loop shape
to `R_DrawSpan.Indexed`: the existing renderer class invoked an exact typed
span loop, retaining the original Java implementation as a fallback. The
combined masked+span candidate passed all 1,500-tic exactness and recovery
checks.

It did not improve OCI performance. Dense windows were 50.633/49.588 ms
versus 48.767/48.249 ms for masked-only, and plane time remained effectively
flat. The existing `ExactIndexedSpan` generated shape is already preferable;
`0041` is rejected.

## Bound wall-array view experiment

A fresh debug-named profile of the masked-only lineage ranked TeaVM's
`otji_JS_wrap` adapter first at 16.687 percent of sampled CPU. `0042` tested
the direct hypothesis: bind stable screen, clip, view-angle, texture, light,
plane, masked-column, and state typed-array views once, then invoke each wall
segment with scalar IDs rather than repeatedly wrapping Java arrays.

The candidate remained exact over 1,500 tics, including raw checkpoint bytes
and cross-artifact recovery. It was nevertheless slower on OCI:

| Metric | Masked-only | Bound views |
|---|---:|---:|
| dense total 101–200 | 48.767 ms | 50.742 ms |
| dense total 201–300 | 48.249 ms | 49.724 ms |
| dense BSP 101–200 | 27.980 ms | 29.845 ms |
| dense BSP 201–300 | 27.111 ms | 28.529 ms |

Registry lookups, the larger generated shape, and scalar dispatch outweighed
the eliminated wrappers. Array wrapping is a real aggregate profile cost but
is not an independently profitable wall optimization in this shape. `0042`
is rejected and was not deployed.

## Full StoreWallRange source split

`0043-teavm-store-wall-range-full-source-split.patch` decomposed the complete
Java `StoreWallRange` implementation into smaller single-sided, two-sided,
lighting/offset, step, and finalize methods. This tested whether Oracle's MLE
compiler was failing to optimize the original large generated function.

The candidate remained exact for 1,500 tics, with identical frame and state
chains and all checkpoint/restore comparisons passing. It was decisively
slower on OCI: the dense totals rose to 53.054/56.203 ms and BSP alone rose
to 30.727/32.277 ms, versus 48.767/48.249 and 27.980/27.111 ms for the
masked-only candidate. Source-level method repartitioning is rejected.

## Direct concrete SegDrawer dispatch

`0044-teavm-direct-segdrawer-dispatch.patch` cast the BSP event replay target
once to the concrete `SegDrawer` type and marked its geometry and wall-range
entry points final. It passed the 1,500-tic exact differential, including all
raw and cross-artifact checkpoint restores.

The paired OCI result was measurement noise:

| Metric | Masked-only | Direct dispatch |
|---|---:|---:|
| all-window total mean | 32.583 ms | 32.557 ms |
| all-window BSP mean | 17.762 ms | 17.752 ms |
| dense total 101–200 | 48.767 ms | 49.381 ms |
| dense total 201–300 | 48.249 ms | 48.183 ms |

One dense window regressed and the other moved by only 0.066 ms. Direct
dispatch is rejected and was not deployed. These results close the remaining
small generated-code adjustment path: the next candidate must eliminate a
substantial portion of the per-wall object/control pipeline rather than
reshape it.

## Fused wall ordering experiment

`0045-teavm-fused-wall-drawseg-tape.patch` moved the exact wall setup,
column raster, clip mutation, plane marking, and drawseg clip snapshots into
a typed command path. A frame-deferred first version diverged at tic 1.
The mismatch was localized to an original Doom dependency: a later
`StoreWallRange` observes the clip and visplane mutations produced by earlier
walls.

`0046-teavm-fused-wall-immediate-flush-diagnostic.patch` exposed each wall's
mutations before the next wall setup. This form passed 1,500 tics with
identical frame/state chains, three raw checkpoint matches, and all
own/cross-artifact restores. It therefore proves the fused wall arithmetic,
texture sampling, clipping, and drawseg snapshots are exact.

The exact immediate form is not viable: OCI dense windows rose to
66.706/63.598 ms, with BSP at 34.911/33.548 ms. Three crossings per wall and
the retained Java `StoreWallRange` control path cost more than the original.
The frame-deferred form cannot be repaired by merely moving clip copies; the
typed pass must own the inter-wall clip/visplane dependency and materialize
only the final plane/drawseg state back to Java.

## Whole-visplane typed raster experiment

`0047-teavm-whole-visplane-raster.patch` transcribed `R_MakeSpans`,
`R_MapPlane`, and the exact indexed flat loop into one typed call per regular
visplane. A first exactness failure exposed a TeaVM minifier collision between
an outer fixed-colormap offset and an inner scanline parameter. Capturing the
offset under a distinct lexical binding corrected it; the Java and JavaScript
first-span geometry then matched field-for-field.

The corrected candidate passed the full 1,500-tic differential with identical
frame/state chains and checkpoint recovery. It nevertheless regressed on OCI:

| Metric | Masked-only | Whole visplane |
|---|---:|---:|
| dense total 101–200 | 48.767 ms | 58.262 ms |
| dense total 201–300 | 48.249 ms | 53.927 ms |
| dense planes 101–200 | 10.719 ms | 13.654 ms |
| dense planes 201–300 | 10.506 ms | 12.446 ms |
| dense BSP 101–200 | 27.980 ms | 34.284 ms |
| dense BSP 201–300 | 27.111 ms | 30.818 ms |

The per-plane typed crossing does not beat the existing `ExactIndexedSpan`,
and the enlarged generated shape also harms BSP. This path is rejected and
was not deployed.

## Full typed BSP/wall-state pass

`0049-teavm-typed-bsp-wall-state-pass.patch` implemented the remaining
structural exact-render hypothesis. One typed MLE call owns subsector
`FindPlane`, wall `CheckPlane`, texture rasterization, inter-wall clip
mutation, opening allocation, and drawseg snapshots; Java receives only the
final visplane and drawseg state required by the unchanged plane/masked
stages.

The implementation exposed and closed four concrete parity defects before
measurement:

- the compact tape's distance/scale/scale-step fields were initially read
  from adjacent slots;
- an untextured wall has no light-table binding;
- valid masked-texture opening pointers may be negative and therefore cannot
  use sign as a null sentinel;
- `CheckPlane`'s returned split-plane ID must become the current subsector
  plane for the next wall.

A diagnostic stage comparison then matched the old renderer at every
boundary: the BSP framebuffer, 117 drawsegs, 889 opening values, 52
visplanes, the post-plane framebuffer, and the final framebuffer. The
instrumentation-free artifact
`7dda0bdf818fe0feb8b6f1184065b6e9b016ae3068e08e4610e39d1f1f8f0213`
passed both 300- and 1,500-tic differentials with zero frame/state mismatch,
raw checkpoint identity, and own/cross-artifact restore identity.

OCI performance rejected it:

| Window | Masked-only total | Full typed total | Full typed BSP | Full typed planes | Full typed masked |
|---|---:|---:|---:|---:|---:|
| 1–100 | historical baseline not used for promotion | 24.782 ms | 12.943 ms | 9.162 ms | 2.515 ms |
| 101–200 dense | 48.767 ms | 60.133 ms | 28.636 ms | 20.474 ms | 10.816 ms |
| 201–300 dense | 48.249 ms | 59.437 ms | 28.067 ms | 19.406 ms | 11.759 ms |

The pass is excellent at low command density but loses at peak density and
also perturbs compilation/layout of the unchanged downstream stages. It is
therefore rejected for production despite exactness. The diagnostic module,
fixture table, and call specs were removed; the production source remained
`66dd235cde82a8b8fbcac88bb905912bacfd6ea40671d2808e5951ce290ce873`,
and both public warm slots returned to `READY`.

## Current decision

The public OCI deployment remains the proven exact high-fidelity renderer
(`51aadc21bcb619928cee3e73217c8150cebf19aa3186dfd273ea3535d88e2edb`),
at approximately 19.6 FPS in its measured dense moving/firing windows. The
exact in-place masked-column change is retained as a building block and
raises a candidate to approximately 20.5–20.7 FPS, but is not independently
large enough to justify a production promotion.

The final structural exact-wall hypothesis has now also been implemented,
made exact, and rejected by dense OCI evidence. The current exact high-detail
ceiling on Always Free is therefore approximately 20.5–20.7 FPS with the
accepted in-place masked-column building block; no measured exact
single-session renderer shape reaches 30 FPS.

Further work must change a larger execution property rather than rearrange
the same raster loops: improve compiler digestibility without enlarging the
generated presentation artifact, divide work across an independently
available execution resource, or adopt a database-generated live frame
policy that preserves Doom's visible behavior without demanding a complete
fresh high-detail raster on every presented frame. The rejected compact
custom rasterizer remains out of consideration because it did not preserve
Doom's visuals.

## Public-contract post-link follow-up

A fresh 30-second public-path measurement of the deployed `51aadc21...`
artifact produced 20.459 FPS and is the promotion baseline for subsequent
post-link candidates.  This supersedes the older approximate 19.6 FPS
description above for incremental deployment decisions.

The shape-preserving wall-helper candidate hoisted the precomputed wall
path's nested `fixedMul` and four-pixel-unrolled `drawColumn` helpers to
module scope without rebuilding the rest of TeaVM's output:

- candidate SHA-256:
  `8ac4762887b443f323c2644ce558e8142923458c454fde24bd1c171b59f499d2`;
- 1,500-tic frame/state/checkpoint differential: PASS, zero mismatches;
- public run A: 19.994 FPS, 43.4/87.1 ms gap p50/p95;
- public warm repeat: 19.357 FPS, 43.9/86.0 ms gap p50/p95.

Both public results were below 20.459 FPS.  The candidate was rejected and
rolled back.  Production was reverified with two READY retained slots, zero
FAILED slots, and renderer SHA `51aadc21...`.  Node self-time attributed to
a nested generated helper is therefore not, by itself, evidence of an OCI
MLE win; every change continues to require an exact differential followed by
a public end-to-end A/B before promotion.

## Exact-keyframe temporal solo stream

The remaining single-session raster cost was addressed without replacing or
lowering the fidelity of the proven renderer.  Solo play now keeps complete
frames from renderer `51aadc21...` as exact keyframes and creates intervening
320x200 indexed frames inside the MLE coordinator.  Every transport tic still
has a database-authored framebuffer; the browser remains a pixel decoder and
canvas blitter.

The OCI kernel ranking established the cost of one synthesized frame:

| Kernel | p50 | p95 |
|---|---:|---:|
| four-pixel spatial selection | 3.708 ms | 3.804 ms |
| pixel checkerboard (`Uint32`) | 2.792 ms | 2.881 ms |
| alternating scanline bulk copy | 0.183 ms | 0.199 ms |
| full 256x256 palette-LUT blend | 8.410 ms | 8.495 ms |
| eight-word-unrolled checkerboard | 2.708 ms | 2.844 ms |

The first interval-2 coordinator (`37aa0747...`) used alternating exact
keyframes and pixel checkerboards.  A real Mocha/FreeDoom integration test
verified seven consecutive DPD1 records and every synthesized pixel.  Public
results were 29.271 and 27.859 FPS, already 36-43% faster than the frozen
20.459 FPS control.  Instrumented playout then localized the remaining loss
to sustained source starvation: 122 starvations, buffer p50 one frame, and
858/859 presentations in deceleration mode.

The current interval-3 coordinator keeps one exact keyframe every three tics
and produces the 1/3 and 2/3 spatial-phase frames in MLE.  Its pinned
candidate identity is:

- renderer: `51aadc21bcb619928cee3e73217c8150cebf19aa3186dfd273ea3535d88e2edb`;
- coordinator:
  `d3d0571ac93f594f340b61b51545c97be73046535b1204c87769e9d2b15f4b84`.

The Node integration gate again produced seven consecutive records and
verified every synthesized pixel.  The first public 30-second run measured
**33.059 FPS** (992 frames, 31.3/33.0 ms gap p50/p95), versus 20.459 FPS for
the exact-every-tic control.  This is a 61.6% throughput improvement and the
first public database-framebuffer result above the requested 30 FPS.  It is
currently deployed for visual evaluation at:

`https://G53C2244DAB9063-DOOMDB.adb.us-ashburn-1.oraclecloudapps.com/ords/doom/app/`

The result does not claim byte-exact intermediate frames.  Exact keyframes,
simulation authority, command ordering, title/menu assets, HUD composition,
and all database/client authority boundaries are unchanged.  The open
acceptance work is visual review of motion/gun/sprite/door/palette behavior,
a warm repeat and longer run, tail-stall attribution, recovery/generation
reset coverage, and an explicit multiplayer regression cell (multiplayer
continues to use the exact-every-tic path).

## Held-input and material-canvas correction

User review exposed that the original FPS marker counted presentation calls,
not materially different canvas contents. That metric is no longer accepted
alone. The public harness now holds `ArrowUp`, applies deterministic left and
right intervals with firing disabled, fingerprints both the 64,000-byte
database framebuffer and the actual 320x200 canvas backing store, and counts
the number of changed indexed pixels between consecutive frames.

The deployed route produced 32.620 FPS, 324 distinct database and canvas
buffers out of 327 presentations, a 14,280-pixel median consecutive change,
and 310 material frames (at least 1,000 changed pixels). Both the browser
command event and the authoritative effective-input response were observed.
Separate retained-engine evidence measures a 1,142,819-fixed-unit displacement
under held forward input and an angle change under held turn input. Captured
public frames show the player leaving the E1M1 start room and rotating through
the corridor; light/weapon animation alone cannot satisfy these assertions.

The public client was republished as
`multiplayer-c3c2014586cc.js`
(`c3c2014586cc6ef51101c4c5d88beef276eaa8602ddcbe12c2abbdddae82d96e`).
Its HUD exposes the currently captured input plus Oracle's effective tic and
input round-trip, so a browser-specific failure can be classified in the
failing session rather than inferred from frame cadence.

## Forward-only public reproduction and effective-tic fence

The reported symptom was narrowed further with a forward-only public run:
no firing and no turning. Holding `ArrowUp` for twelve seconds produced
33.886 FPS, 390 distinct database and canvas buffers across 416
presentations, and 382 materially different frames. The captured canvas left
the E1M1 starting room, passed through the doorway, and reached the opposite
wall. Only two of 274 pixel-exchange requests were intentionally cancelled
as completed-request tails; none failed.

The authenticated OCI endpoint gate now follows Oracle's returned
`p_effective_tic` rather than assuming that a requested tic is necessarily
the effective tic. It also decodes the production gzip-wrapped DPB2 payload,
keeps presence alive while wallet-backed diagnostics run, and accepts
publication batches split at any transport boundary. In the passing run,
forward input was requested for tic 20 and became effective at tic 20; the
database framebuffer at that tic differed from the immediately preceding
frame in 8,495 indexed pixels. The endpoint and lifecycle audit passed
against authority `66dd235c...`, renderer `51aadc21...`, and coordinator
`d3d0571a...`.

This closes repeated-frame behavior as a general server-path explanation.
The HUD remains the browser-session classifier: `INPUT IDLE` while a key is
held identifies capture/focus loss; an effective input tic with a stalled
`DB FRAME` identifies transport/playout; advancing DB frames with a static
canvas identifies the final blit.

## Held-forward stall reproduction and transport fix

The reported 10-30 second symptom was subsequently reproduced with the public
client under a 200 ms RTT / 10 Mbps downstream profile. Oracle accepted the
held-forward revision and continued producing changing full framebuffers, but
the client rendered only 24.783 FPS and recorded a 7,036.9 ms maximum canvas
gap. Eighty-three of 232 pixel requests were canceled.

Two client transport defects were responsible:

1. the 120 ms tail hedge treated ordinary WAN latency as a stranded request,
   duplicating reads against Always Free's single guaranteed API lane and
   canceling the losing copy;
2. after the finite 64-frame ring overran, the client waited for a full
   startup reserve before painting the new confirmed suffix. A
   bandwidth-limited client could overrun again before building that reserve,
   turning one recoverable gap into repeated resynchronization and a frozen
   canvas.

The deployed client now launches a hedge only after 750 ms and paints the
first available confirmed database frame immediately after a genuine ring
overrun, rebuilding reserve through the occupancy controller. It still never
predicts, invents, or reorders a frame. Under the same stressed profile,
ordinary duplicate cancellations fell from 83 to zero in the isolated hedge
cell, and the resynchronization change reduced the worst observed canvas gap
from 7,036.9 ms to 855.5 ms. The 10 Mbps profile remains bandwidth-limited
and is not a 30 FPS qualification result.

The final normal-path public rerun passed with:

- 34.234 FPS across 523 canvas presentations;
- 28.2/32.6 ms gap p50/p95 and 99.5 ms maximum;
- 520 distinct database and canvas buffers;
- 519 consecutive database and canvas changes;
- 487 materially changing frames;
- five held-forward/turn input transitions and five authoritative effective
  revisions;
- 308 successful pixel exchanges, zero canceled, and zero failed.

Both retained slots were rebuilt and ended `READY`. The current public client
entry is `multiplayer-cc57ca35a233.js`; its database-frame source remains the
pinned `66dd235c...` authority, `51aadc21...` exact renderer, and
`d3d0571a...` temporal-solo coordinator.

## Two-view temporal diagnostics

The multiplayer target was explicitly separated from solo: solo retains its
30 FPS database-frame gate, while two simultaneous independent viewpoints may
qualify at 20 FPS. The acceptance harness now accepts only the explicit values
20 or 30 and derives its cadence tails from that selected budget; 30 remains
the default.

Neither measured candidate qualified:

| Candidate | Exact keyframe interval | Player 0 | Player 1 | Verdict |
|---|---:|---:|---:|---|
| `0c2e1c7d...` | 2 | 16.476 FPS | 16.516 FPS | reject |
| `a65fee9e...` | 3 | 18.786 FPS | 18.796 FPS | reject |

The interval-3 p95 gaps were 148.7 and 161.5 ms. All 300 captured
framebuffers for each player were unique, consecutive, database-authored
frames, and common-tic POV digests remained different. The failure is
therefore producer burst/starvation, not a repeated-frame or collapsed-POV
defect. The candidates were not promoted; production was restored to the
pinned `d3d0571a...` coordinator after the diagnostic.

One retained coverage item is explicit: when a palette or layout changes, the
temporal fallback intentionally copies the newer exact pixels for the
intermediate tics. No such transition occurred in these 300-frame cells, so a
damage/pickup-flash route must separately verify whether adjacent visual
duplicates are acceptable or need palette-aware synthesis.

An independent xhigh audit confirmed the burst shape from raw samples.
Interval 2 alternated roughly 17 ms and 104–107 ms gaps; interval 3 delivered
roughly 17, 31, then 96–112 ms. The browser's selected reserve never became a
real reserve: it repeatedly drained to zero, while 622 pixel exchanges served
about 200 useful interval-3 bundles.

Three narrower remedies were measured and rejected:

- a multiplayer-only 49 ms clock, three-frame reserve, starvation deadline
  reset, and bundle-aware poll delay reduced empty polling but left production
  at 18.73 FPS with a 101.0 ms p95 gap;
- persisting one DPB2 bundle per player reduced temporal locator calls from
  three to two, but regressed to 17.47 FPS because viewpoint extraction and
  response tails grew;
- interval-4 exact keyframes plus four-tic-aware polling reached only
  18.38 FPS and a 95.1 ms p95 gap.

The near-flat interval-3/interval-4 result shows that separate 128,016-byte
DPD1 persistence for every synthesized tic is now a first-class ceiling.
Lowering exact-render frequency again is not justified. The remaining
high-fidelity persistence experiment is one combined multi-tic/two-POV
locator, retaining the old exact renderer at its keyframes and extracting the
authenticated player view only on the read side.

## Native temporal synthesis and effective-input presentation

The combined DPV2 locator removed the per-view/per-tic locator multiplier.
Its remaining keyframe tail was then decomposed: publication averaged only
about 1.6 ms/tic, while interpreted temporal pixel synthesis was still inside
the worker's long preparation stage. Oracle-native `UTL_RAW` bit operations
can reproduce the exact two spatial phases without a JavaScript pixel loop.
The same two-POV, two-intermediate-frame operation measured 0.513 ms per
bundle on ADB (0.257 ms per synthesized frame).

The deployed candidate therefore writes exact EPT1 endpoints in the retained
MLE transaction and calls `materialize_temporal_bundle`, which validates the
header and metadata, synthesizes both intermediate views with native RAW
operations, and replaces EPT1 with the authenticated DPV2 bundle before the
transaction commits. SQL and Node fixtures verify every output byte for both
views and reject malformed inputs. The deployed coordinator is
`55d86b81e6e76ee4416622a59052526481ff2d14536b68f2535ca291b246d85b`.

This raised the dense two-browser source rate to 20.35/20.50 FPS and preserved
300/300 unique frames per viewpoint. A multiplayer-only 20 FPS browser clock
made the visual gaps materially smoother (p95 65.2/72.1 ms instead of about
145 ms), but its 19.50/19.94 FPS averages and input tail do not yet qualify
the two-player path. It remains an incremental public diagnostic while the
remaining read-side compression repetition is investigated. Solo continues
to use the native 35 Hz clock.

A clean public held-forward trace then exposed a separate input-display
problem: Oracle accepted ArrowUp in 56 ms at tic 128 while the canvas was
still painting tic 112. The client was correctly but unhelpfully replaying 16
confirmed pre-input frames before showing movement. The deployed client now
advances its presentation cursor to the last tic before Oracle's returned
effective tic whenever a command changes. Every omitted tic is already
confirmed and is emitted as `pixel-confirmed-drop`; no client simulation or
pixel invention is introduced.

Two post-deploy public runs passed:

- 34.306 FPS, 28.3/32.3 ms gap p50/p95, 198.6 ms maximum;
- 32.749 FPS, 28.3/32.6 ms gap p50/p95, 278.7 ms maximum;
- the exact effective movement frame reached the canvas in 248.5 and
  282.8 ms, versus 398.1 ms before the fix;
- 12 and 16 stale confirmed frames were explicitly skipped;
- the first effective movement frames changed 12,340 and 12,927 indexed
  pixels, and captured canvases show the player leaving the E1M1 start room;
- all scored exchanges in the final run returned HTTP 200.

The public entry now loads `multiplayer-b6681bb763b0.js`.

## Movement-frame verification and endpoint-wire rejection — 2026-07-31

The effective-input catch-up is intentionally solo-only. In solo it moves the
presentation cursor to the exact effective command tic without inventing
pixels. Multiplayer retains every confirmed frame because rapidly changing
commands made a future-tic cursor produce repeated presentation pauses.

The public held-forward/turn route produced direct evidence that movement is
not repainting one framebuffer:

- 474 presentations over 15 seconds at 30.999 FPS;
- 471 unique database fingerprints and 471 unique canvas fingerprints;
- 470 consecutive database and canvas changes;
- 448 frames changed at least 1,000 indexed pixels;
- ArrowUp became effective at tic 101 and that exact frame painted in
  248.8 ms with 12,340 changed pixels;
- all 363 authenticated pixel exchanges returned HTTP 200.

A proposed DPT1 endpoint wire format retained the exact three-frame DPV2 body
in Oracle while returning only its previous/current endpoints. The database
and client exactness fixtures passed, but the real two-browser route fell to
17.29 FPS versus the prior 19.60 FPS. Moving phase expansion onto the browser
main thread did not compensate for transport tails. The cell is classified
`REJECTED_MEASURED_REGRESSION`; the deployed package was restored to the
proven database-expanded DPV2 response immediately.

The final post-rollback 10-second motion sample still proved 289/289 unique
database and canvas frames and a 193.8 ms ArrowUp-to-paint result. One
intermittent HTTP 555 produced a 442.9 ms gap and reduced that short sample to
28.777 FPS. The movement/same-frame defect is closed; attribution and removal
of that intermittent exchange failure remains the current performance work.

## Intermittent pixel-exchange 555 closure — 2026-07-31

A direct 1,200-call database poll reproduced the public 555 as
`ORA-20796: persistent DPD1 view mismatch`. The DPV2 query and legacy DPD1
fallback are separate READ COMMITTED statements. If the worker committed a
DPV2 bundle between those statements, the fallback could see the new row and
incorrectly pass it to the DPD1 validator.

The fallback now filters explicitly on DPD1 magic. A DPV2 bundle that becomes
visible in that window is returned by the immediately following poll instead
of becoming an HTTP 555. The local and ADB SQL fixtures include the
foreign-format/race shape and retain the malformed-DPV2 fail-closed test.

Post-deploy evidence:

- a 60-second stress route made 1,053 pixel exchanges, all HTTP 200;
- a separate held-forward/turn route made 577 exchanges, all HTTP 200;
- that 30-second movement route sustained 32.855 FPS;
- all 986 database fingerprints and all 986 canvas fingerprints were unique;
- ArrowUp's effective frame reached the canvas in 180.7 ms and changed
  12,658 indexed pixels.

The 60-second route also kept 1,090 of 1,091 frames unique but sustained
firing increased awake/combat work and reduced delivery to 18.164 FPS. That
is now classified separately as a density-dependent render/authority
throughput shortfall; it is not transport corruption or repeated-frame
behavior.

## Physical-movement triage and multi-locator transport — 2026-07-31

The 18.164 FPS firing result was not an authority-density limit. The client
treated every automated fire press and release as a camera-response event and
repeatedly discarded already-confirmed presentation history. Presentation
catch-up is now limited to movement, turning, use, weapon, and UI changes;
fire still advances through every authoritative weapon-animation frame. A
comparable public firing route first recovered to 30.205 FPS.

One authenticated ORDS exchange could previously return at most one
three-frame DPV2 locator. Network tails therefore capped a single-flight
client even when several consecutive locators were committed. The read facade
now validates and aggregates consecutive DPV2 locators into its existing
eight-frame response contract. No persistent format or client authority rule
changed. The SQL fixture proves multi-locator ordering, maximum-count
truncation, and a fail-closed sequence fence. The post-deploy firing route
then sustained 33.171 FPS with 994 of 996 frame fingerprints unique and
521/521 exchanges returning HTTP 200.

The first two-view run exposed three-frame duplicates only across palette
transitions. Temporal materialization had treated palette identity as a pixel
layout constraint and copied the newer keyframe for both intermediate tics.
The native temporal phases are now synthesized across palette changes while
each phase retains the correct endpoint palette; only an actual layout change
uses the exact-keyframe fallback. Local and ADB fixtures verify both
intermediate frame byte arrays and palette bytes.

Fresh public movement after that deployment directly refuted a repeated-frame
or unpainted-canvas failure:

- 1,012 presentations over 30 seconds at 33.502 FPS;
- 1,012 unique database fingerprints and 1,012 unique canvas fingerprints;
- all 1,011 consecutive frame pairs changed;
- ArrowUp's effective frame painted in 263.9 ms and changed 13,124 indexed
  pixels;
- 584/584 pixel exchanges returned HTTP 200 with no cancellation;
- captured canvases show the player moving and turning out of the start room.

The two-view uniqueness defect is also closed: both players produced 300
unique tics and 300 unique POV framebuffers. The 20 FPS multiplayer throughput
floor was narrowly met at 20.71/20.87 FPS, but cadence and input-latency tails
still failed (roughly 100--108 ms p95 gaps, about 469 ms input p95, and one
870 ms service tail). Multiplayer therefore remains unqualified even though
its database-frame identity and average throughput now pass.

## Held-input recovery and final public movement proof — 2026-07-31

A user could begin holding Forward while the retained game was still attaching
its input listener. The first event observed by the new page would then be a
browser repeat, and the former unconditional repeat suppression discarded it.
The input binder now accepts one repeat only when that control is not already
held; later repeats remain suppressed. An executable browser fixture proves
repeat-to-forward recovery, redundant-repeat suppression, and keyup-to-neutral.

The database-frame client also stopped launching a BLOB exchange at the same
instant that it marked an input revision urgent. Frame acquisition resumes
only after the authoritative input call has completed, giving input the next
Free-tier API lane.

The final post-deploy, pinned interval-3 public route proved:

- 1,036 presentations over 30 seconds at 34.294 FPS;
- 1,002 unique database fingerprints and the same 1,002 canvas fingerprints;
- 1,001 consecutive database and canvas changes;
- 87/87 ArrowUp revisions became effective;
- the first effective ArrowUp frame painted in 158.1 ms and changed 20,204
  indexed pixels;
- 577/577 exchanges returned HTTP 200 with no cancellations.

An interval-4 multiplayer diagnostic improved aggregate delivery from roughly
20.7 to 25 FPS and reduced a representative paint p95 from 108 ms to 49 ms,
but its larger publication bursts worsened input and shallow-reserve variants
introduced 409--425 ms starvation tails. It was rejected. OCI was restored to
the pinned `55d86b81...` interval-3 coordinator before the final public proof.

## User-reported static movement triage — 2026-07-31

Fresh, cache-cold public runs reproduced the exact hold-Forward action. A
forward-only route painted 955 distinct database and canvas frames out of
1,044 at 34.546 FPS; its first effective movement frame arrived in 221.9 ms
and changed 11,861 indexed pixels. It then reached a solid wall in roughly two
seconds, after which lighting continued to animate while translation correctly
stopped. That route's former whole-run material-motion assertion therefore
reported a false failure even though transport and movement both passed.

A forward-plus-turn traversal removed that collision ambiguity:

- 482 presentations over 15 seconds at 31.614 FPS;
- 480 unique database and canvas fingerprints;
- 479 consecutive database and canvas changes;
- all five held-Forward command revisions became authoritative;
- the first effective movement frame painted in 244.6 ms and changed 12,966
  indexed pixels;
- 318/318 exchanges returned HTTP 200 with no cancellation or failure.

The public immutable launcher and every mutable imported module also had the
intended cache policy (`immutable` only for the content-addressed launcher;
`no-cache, must-revalidate` for `input.js`, `api.js`, and peers). The evidence
therefore rejects repeated database frames, a stale served input module, and
an unpainted canvas as venue-wide causes. Existing tabs still require a reload
to instantiate a newly deployed module graph. The movement gate now turns by
default so collision with the first wall cannot masquerade as a frozen frame
transport; the collision-only diagnostic remains available with
`DOOMDB_PUBLIC_TURN=NO`.

## Default-standby origin elision and checkpoint attribution — 2026-07-31

A post-deploy replay finally reproduced the remaining user-visible freeze:
default solo averaged 28.527 FPS but stopped painting for 1,949.6 ms at tic
179, and three pixel exchanges were cancelled. This occurred before the first
gameplay checkpoint.

The retained-slot lifecycle established the cause. Every READY slot is already
restored and SHA-fenced at the default skill-3 E1M1 origin. After admitting
the authority, `run_standby_core` nevertheless restored that identical bank
again. On Always Free, authority plus restoring standby consume both runnable
database-session slots, temporarily leaving no ORDS return lane.

The standby now reuses its already-fenced state only for the exact default
tuple (two players, co-op/solo, skill 3, episode 1, map 1). Non-default skill,
map, and deathmatch assignments retain the existing bank restore. A fresh
public A/B after deployment proved:

- 1,011 presentations over 30 seconds at 33.459 FPS;
- 1,008 unique database and canvas fingerprints;
- 1,007 consecutive database and canvas changes;
- 598/598 exchanges returned HTTP 200 with no cancellation or failure;
- ArrowUp's effective movement frame painted in 233.3 ms and changed 12,029
  indexed pixels;
- the maximum canvas gap fell from 1,949.6 ms to 257.3 ms.

The same run crossed the durable tic-512 checkpoint. New sparse phase
telemetry attributed its 12,926.7 ms asynchronous standby build as:

- warm restore: 381.363 ms;
- deterministic replay plus deliberate per-tic yields: 12,420.610 ms;
- DMC1 serialization and export: 123.217 ms;
- SQL publication/commit: 1.519 ms.

This refutes checkpoint serialization as the two-second early-session freeze.
The paced asynchronous checkpoint completed while the single-player browser
still passed 30 FPS. Separate two-view evidence continues to show a different
limiter: two exact POV keyframes publish about every 149 ms, yielding roughly
20--22 FPS even before checkpoint work begins.

The cloud destructive recovery gate then killed the generation-1 retained
authority. The default-origin standby path recovered to generation 2 and
resumed database-pixel delivery at tic 3,210. Before the kill, its movement
fixture changed 8,724 pixels at the requested effective tic; after recovery,
the generation fence, DPB2 transport, lifecycle cleanup, artifact SHA tuple,
and invalid-capability rejection all passed.
