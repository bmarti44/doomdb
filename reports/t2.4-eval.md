# T2.4-EVAL deterministic SQL seed evaluator report

Status: **approved by the user on 2026-07-14**. The exact reviewed evaluator
candidate is authorized for T2.4 implementation.

## Route and boundary

`T2.4-EVAL | Terra | high | attempt 1`

This evaluator-author task added only evaluator expectations, an independent
WAD/texel oracle, visible checks, mutation specifications, pending integrity
metadata, this report, and its routing record. It added no production seed
generator, SQL, or manifest and did not edit `verify.sh`. Approved T0.4, T2.2,
and T2.3 integrity baselines remain byte-for-byte unchanged.

## Candidate delivered

- Eighteen stable test IDs with 168 declared assertions and fourteen semantic
  mutation intents mapped to named kill tests.
- Literal Section 1.1 counts for all eight structured map arrays, REJECT and
  BLOCKMAP bytes, palette 0, COLORMAP levels 0-31, map bounds, and player-one
  spawn.
- Sixteen independently recorded WAD provenance probes with name, zero-based
  occurrence, source offset, size, and SHA-256, including every confined E1M1
  lump and PLAYPAL/COLORMAP/TEXTURE1/TEXTURE2/PNAMES.
- Exact closure equality against the approved T2.3 `asset-closure.json`. Adding
  a real but unrelated WAD asset fails, as does omitting a closure asset.
- Independent last-occurrence source resolution, raw asset hashes, and decoded
  signed-int16 texel hashes for flats, patches, sprites, UI, and composed walls.
- Sixteen hand-recorded spot texels spanning palette RGB, COLORMAP, flats,
  texture composition, sky, UI, and sprite transparency. These are literals in
  evaluator expectations, not generated by production.
- A closed output/interface contract: ASCII, LF-only, final LF, no volatile
  fields, at most 500 logical rows per statement, complete file metadata, and a
  canonical SQL tree hash.
- Two clean temporary-directory executions whose complete path and byte trees
  must be identical. The manifest must enumerate every SQL file and no other
  generated file type is permitted.
- Seeded provenance row totals and SQL-presence checks for every source SHA and
  every exact `(kind,name)` closure asset.

The asset `texelSha256` representation is deliberately unambiguous: row-major
signed int16 little-endian palette values, with transparent pixels represented
as `-1`. This avoids conflating transparency with palette indices 0 or 255.

## Evaluator self-check evidence

```text
node evaluator/t2.4/self-check.mjs
PASS T2.4-EVAL-SELF-CHECK (65/65 fixture-contract assertions)
```

`run-visible.mjs` intentionally fails closed until the approved T2.3 production
documents and the later T2.4 generator exist. It then invokes the generator
twice and validates the full contract. Mutation execution is intentionally
deferred until an implementation exists.

## Approval review points

Please review the generator CLI and manifest shape in
`evaluator/t2.4/README.md`, the exact closure-only rule, signed-int16 texel hash
encoding, 500-row interpretation, literal provenance/hash/texel expectations,
and the two-directory tree-hash rule. Approval authorizes only files hashed by
`evaluator/integrity.pending-T2.4.json`; earlier evaluator baselines stay
immutable.

## Approval record

The user explicitly approved this T2.4 evaluator candidate on 2026-07-14 with
the statement: `approved, continue`. The approval metadata in
`evaluator/integrity.pending-T2.4.json` records that authorization without
changing any reviewed file or hash. Candidate-era approval labels inside hashed
files remain unchanged so the approved candidate stays byte-for-byte identical
to the version the user reviewed.
