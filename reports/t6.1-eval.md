# T6.1-EVAL candidate report

Status: **APPROVED BY USER 2026-07-14 (standing approval)**

Route: Sol high, attempt 1. This task requires an independent Appendix F
transaction-order oracle, canonical state hashing, rollback semantics, and real
concurrent-session validation.

The user explicitly granted standing approval for all remaining evaluator
candidates. The exact reviewed candidate manifest
`8a7d7b32c001a2317abdb09361e843fdad94fc59e9c06dae194194c304f06621`
was therefore promoted after the complete audit below, without changing any
T6.1 evaluator logic, fixtures, expectations, or production source.

Promoted freeze SHA-256:
`8d849ad213682794e8d96432942a5cb1e333b96f545fa4a40ef283f7654e8f5a`.

## Fixed interface and behavior

The candidate fixes internal `DOOM_TIC_TX.APPLY_BATCH(p_session, p_commands,
p_response)`. It owns no transport surface and leaves commit/rollback to its
caller. The full reviewed canonical JSON, SHA-256, state-document, event-order,
35-Hz, and application-error contracts are in `evaluator/t6.1/README.md`.

The evaluator requires the session row lock before JSON expansion, consecutive
one-to-four command batches, one tic per command, canonical per-command and
batch hashes, dense control-event ordinals, one canonical `STATE_HISTORY`
snapshot per accepted batch, and payload construction/cache persistence before
the caller can commit. An exact last-batch retry returns byte-identical cache
content and changes no rows. Conflict, old, gap, malformed, and payload failure
paths apply none.

## Independent oracle and adversarial coverage

The JavaScript oracle imports no production source. Its complete fixture state
contains every player column and ordered relational collections; random session
identity, timestamps, expiry, and append-only transport/cache history are
excluded from logical state hashes. Token variation and collection reordering
prove stable keys rather than identity, ROWID, or insertion order own the hash.

The live concurrency harness launches two real SQL*Plus sessions twice. The
identical race must return the same response hash and apply once. The conflicting
race must produce exactly one success and one `ORA-20862`, also applying once.
Crashes, incidental errors, absent result markers, or duplicate rows fail.

The manifest has 20 stable IDs and 430 declared assertions. Eighteen isolated
mutations cover lock deletion, gaps, nonconsecutive arrays, retry re-execution,
raw JSON hashing, wall-clock tics, wrong cadence, RNG drift, event reordering,
duplicate ordinals, partial malformed input, conflict/old misclassification,
token/ROWID hashing, premature commit, procedural shadow simulation, and
evaluator coupling.

## Commands and results

```text
node evaluator/t6.1/self-check.mjs
PASS T6.1-EVAL-SELF-CHECK (70/70 fixture-contract assertions)

node evaluator/t6.1/mutation-self-check.mjs
PASS T6.1-EVAL-MUTATION-SELF-CHECK (18/18 isolated mutations killed)

bash -n evaluator/t6.1/run-visible.sh evaluator/t6.1/run-concurrency.sh
PASS (shell syntax)

node evaluator/run-foundation.mjs
PASS T0.4 (8/8 assertions)

node evaluator/self-test.mjs
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)

candidate and inherited integrity verification
PASS T6.1-CANDIDATE-INTEGRITY (18/18 paths)
```

`source-audit.mjs`, the Oracle production runner, and the concurrency runner are
deliberately fail-closed until approved T6.1 production exists. No production,
earlier evaluator, root verifier, or approved baseline was modified.

Approved freeze: `evaluator/integrity.pending-T6.1.json`.
