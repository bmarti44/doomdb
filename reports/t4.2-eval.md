# T4.2 solid wall/floor/ceiling/texture/light evaluator candidate

Status: **approved by the user on 2026-07-14**. The exact refreshed evaluator
candidate is authorized for T4.2 implementation. No production SQL, approved
T4.1 files, root verifier, or visual golden was modified during approval.

Route: `T4.2-EVAL | Sol | high | attempt 1`.

The reviewed production interface is the bound table SQL macro
`DOOM_R1_PIXELS(p_session VARCHAR2)`, returning exactly 64,000 rows with
`SESSION_TOKEN`, `COLUMN_NO`, `ROW_NO`, `PALETTE_INDEX`, and `LAYER_ORDINAL`.
Canonical hashes cover exactly one palette byte for every `(column,row)` in that
order. Distinct sessions with identical state must return identical frames.

The evaluator independently decodes the pinned WAD, composes wall textures,
reads flats and COLORMAP, traces nearest solid segs, projects half-row pixel
centers, samples negative-safe coordinates, applies offsets/pegging, and maps
sector light. It imports no production parser, seed SQL, renderer, or T4.1
reference. Evidence includes a patterned mini-map and E1M1 spawn facing east,
north, and south, with 48 independent spot pixels/layers and four full hashes.

The manifest contains 20 stable IDs and 384,426 declared assertions. Eighteen
semantic mutations cover dropped/duplicated pixels, row center, projection,
texture offsets, pegging, negative modulus, swapped flats, raw wall texels,
COLORMAP band/light rounding, session isolation, ordering, procedural/dynamic
rendering, embedded frames, and the documented R1 nearest-sector limitation.

Candidate checks:

```text
PASS T4.2-EVAL-SELF-CHECK (139/139 fixture-contract assertions)
PASS T4.2-EVAL-MUTATION-SELF-CHECK (18/18 isolated mutations killed)
PASS T0.4 (8/8 assertions)
```

The live Oracle runner is correctly expected to remain red until this candidate
is approved and production is implemented. The pending integrity manifest now
chains the user-approved T4.1 baseline at SHA-256
`158c94e68220bbea4809f8688cb94549b07423655aaa4017b6fcaf3703c28ae6`,
including its approved syntax correction. The refreshed, still-unapproved T4.2
pending manifest SHA-256 is
`20708e11cdb11bfc2647525b003de76447702dfefbc02f06521f432cbd52e1fa`.

## Approval record

After the T4.1 evaluator correction, the dependent manifest was refreshed and
the user explicitly reapproved SHA-256
`20708e11cdb11bfc2647525b003de76447702dfefbc02f06521f432cbd52e1fa`
on 2026-07-14 with the statement: `great, approved, continue`. Before
promotion, all eleven candidate files and all eight inherited baselines were
recomputed and matched the reviewed manifest (19/19). The frozen evaluator
checks passed exactly:

```text
PASS T4.2-EVAL-SELF-CHECK (139/139 fixture-contract assertions)
PASS T4.2-EVAL-MUTATION-SELF-CHECK (18/18 isolated mutations killed)
PASS T4.2-EVAL-INTEGRITY (19/19 candidate/inherited hashes)
PASS T0.4 (8/8 assertions)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
```

Only the approval label in the integrity metadata was changed. Candidate files,
fixtures, expectations, production code, root verifier, and inherited baselines
remain byte-for-byte identical to the reviewed candidate. The approved
integrity-manifest SHA-256 is recorded below.

```text
5ca1b511b7ebefddee3169a3485248d76afb07325ae7a271d790565842747155  evaluator/integrity.pending-T4.2.json
```

## Approved evaluator-only Oracle syntax correction

The user approved the documented correction on 2026-07-14 and then granted
standing approval for all remaining evaluator corrections and promotions. The
unchanged `SET CONSTRAINTS ALL DEFERRED` statement was moved from inside the
anonymous PL/SQL block to immediately before `DECLARE`. No assertion, test id,
fixture, expectation, hash oracle, production interface, or production file was
changed.

Post-correction evaluator evidence:

```text
PASS T4.2-EVAL-SELF-CHECK (139/139 fixture-contract assertions)
PASS T4.2-EVAL-MUTATION-SELF-CHECK (18/18 isolated mutations killed)
PASS T4.2-SOURCE-AUDIT (1 SQL files; canonical order; no procedural pixel loop or dynamic SQL; no expected frame)
PASS T4.2-EVAL-INTEGRITY (19/19 candidate/inherited hashes)
PASS T0.4 (8/8 assertions)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
```

The corrected source and promoted evaluator manifest hashes are:

```text
b58d3423a5a4b7b67bd8ff5e776cbee590421fd5c80ee5af3d0da810d192f57e  evaluator/t4.2/oracle-production.sql
1cd2021266edea250fd11f9d285a5cdeb3d1fe826c5b557a3d95408d4cd70429  evaluator/integrity.pending-T4.2.json
```

The corrected live runner passed `SET CONSTRAINTS` and reached the real
`DOOM_R1_PIXELS` implementation. Its first dense-frame aggregate remained
CPU-bound for 513 seconds (`SQL_ID 509018q871kg9`, `Allocate UGA memory from
OS`) without returning; an earlier abandoned instance of the same production
query had accumulated approximately 1,890 seconds. At orchestrator direction,
the evaluator session was terminated and rolled back to release the shared
schema for unrelated work. This is production performance evidence for the
T4.2 implementation route, not an evaluator-correction failure; the optimized
implementation must rerun the full frozen live suite in an isolated stack.
