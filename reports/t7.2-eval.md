# T7.2 evaluator report

Status: **APPROVED AND RE-FROZEN — user standing authorization 2026-07-15**

Route: Sol max. This was evaluator-only work. It added no production SQL, did
not inspect unfinished T6.4 or T7.1 production, and did not modify shared
bootstrap, drop, root verification, or routing files.

## Frozen contract

The internal boundary is the unique non-overloaded
`DOOM_MONSTERS.ADVANCE(session_token,tic)` procedure, delegated by the owning tic
transaction after world movement and T7.1 combat outcomes. Input commands never
contain state, perception, target, direction, collision, attack, damage, pain,
death, drop, random, or event results.

Relational `DOOM_MONSTER_DEF` rows cover E1M1 types 9, 58, 3001, 3002, and 3004,
linking independently authored see/chase/melee/missile/pain/death states, speed,
pain chance, attack math, projectile and optional drop. State expiry advances by
`DOOM_STATE_DEF.next_state_id` joins and the graph is sound and closed.

Sound acquisition uses bounded sector-graph reachability with sound-block edges
and a visited set. LOS first applies `DOOM_REJECT_BYTE` as a negative-only filter,
then orders exact rational blocking intercepts. Chase direction, collision,
attacks, pain, death, drops, random reads and events have stable ordering. All
actors advance from one immutable prior-tic snapshot. Every reviewed type has
focused idle/wake, heard/not-heard, seen/occluded, chase, melee, hitscan,
projectile, pain, death, drop, and complete replay-determinism coverage.

## Independent evidence

```text
PASS T7.2-EVAL-SELF-CHECK (70/70 fixture-contract assertions)
PASS T7.2-EVAL-MUTATION-SELF-CHECK (24/24 isolated mutations killed)
PASS T7.2-SOURCE-POLICY-SELF-CHECK (production source not yet present)
PASS T0.4 (8/8 assertions)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
PASS T7.2-INTEGRITY (15/15 candidate and inherited entries)
```

The manifest declares 25 stable IDs and 2,565 assertions. It pins the approved
foundation, final corrected T5.2 evaluator
`62bca74b9f9916a5dfb2c3f1543322356982c3ac1c3c025e5842242ee34cc5fe`,
final T6.3 evaluator, current final T6.4 evaluator, and final T7.1 evaluator
`45be1db5dea63fc4291851ac4e692e9a66bd903096b35bf8b2379a190503f30e`.
No mutable production source is pinned. T5.2, T6.3, T6.4, and T7.1 production
acceptance are recorded green; no upstream production blocker remains.

Frozen source-policy SHA-256:
`00b7b06ea8aed54a4cc092244bd21f29a7a31443bdf0690a8d6ae0c1bb48c2c3`.

Frozen evaluator manifest SHA-256:
`764dda2fbf89bcb02511e6ffc9e3ba877757cfedb9b2d5a8981bb37b444e82e5`.

## Dependency-only refresh

The evaluator was re-promoted on 2026-07-15 after auditing every inherited
pointer. Only dependency statuses, inherited manifest pointers, and this report
metadata changed. The refresh pins final T5.2, final T6.3, current final T6.4,
and the explicitly approved final T7.1 manifest. No T7.2 behavior, fixture,
reference, test ID, assertion, mutation witness, Oracle runner, source policy, or
implementation interface changed. The source-policy self-check was rerun from an
isolated evaluator-only copy so the dependency refresh did not inspect production.

## Implementation handoff

Implement `sql/sim/060_monsters.sql`, reviewed definition/schema changes, and
exactly one `DOOM_MONSTERS.ADVANCE` call in the tic transaction. Required
definition columns are pinned by the Oracle evaluator: speed, pain chance, melee
range, attack kind, damage base/dice, projectile/drop type, and see/chase/pain/
death state links. Authoritative mobjs require sector, move direction, awake and
attack cooldown state.

Preserve evaluator files and manifest byte-for-byte. Run
`bash evaluator/t7.2/run-visible.sh` and prove all 25
IDs / 2,565 assertions plus all 24 mutation witnesses. Production must use graph
sound reachability, REJECT negative filtering, exact ordered LOS intercepts and
relational definitions; hard-coded reviewed type branches fail closed.
