# T10.1 evaluator report

Status: **APPROVED AND FROZEN under user standing authorization 2026-07-15;
live acceptance remains blocked by unfinished upstream and T9.1 production**.

Route: Terra high. This was evaluator-only work. It did not inspect or implement
the production REST package, edit bootstrap/drop/root routing, or start Oracle.

## Frozen interface

The public package has exactly seven unique, non-overloaded procedures in Section
5.4 order: `NEW_GAME`, `STEP`, `SAVE_GAME`, `LOAD_GAME`, `START_REPLAY`,
`STEP_REPLAY`, and `GET_ASSET`, with exact argument ordinals, modes, and SQL
types. Object AutoREST metadata must contain exactly `DOOM_API` and non-updatable
`PUBLIC_HEALTH`. Custom handlers and direct state-table grants/reachability fail.

Raw HTTP acceptance covers POST/content type, gzip magic and canonical field
order, an independently expanded 320x200 palette-byte frame hash, exact cached
compressed response identity, malformed/oversize/key/range/sequence failures,
rollback, same-session collision and independent-session progress, save/load,
bounded replay, exact assets, forbidden methods/routes, health secrecy, and CORS.

## Independent evidence

```text
PASS T10.1-EVAL-SELF-CHECK (38/38 fixture-contract assertions)
PASS T10.1-EVAL-MUTATION-SELF-CHECK (24/24 isolated mutations killed)
PASS T10.1-SOURCE-POLICY-SELF-CHECK (synthetic positive and negative canaries)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
PASS evaluator foundation/adversarial controls
```

The frozen manifest declares 15 stable IDs and 1,457 assertions. Live production
acceptance deliberately does not skip if Oracle, package/health URLs, source,
metadata views, assets, or any endpoint is absent.

## Dependency handoff

The candidate pins the frozen T8.2 and T9.1 evaluator lineages. Their live
production/visual acceptance and the T10.1 implementation are explicit blockers.
Future dependency-only pointer refreshes must not weaken T10.1 behavior,
fixtures, mutations, or direct SQL/HTTP probes.
