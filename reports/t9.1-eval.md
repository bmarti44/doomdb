# T9.1 ordered MODEL fire evaluator report

Status: **APPROVED AND FROZEN under user standing authorization 2026-07-15;
live production and actual-animation visual acceptance are blocked by unfinished
T8.1/T8.2 gates and absent T9.1 production**.

Route: Sol high. This was evaluator-only work. It did not inspect or implement
production, start Oracle, or modify bootstrap, drop, root verification, or
routing files.

## Frozen interface and behavior

Production exposes `DOOM_FIRE.GENERATE`, canonical flattened RLE rows in
`DOOM_FIRE_FRAME_RUN`, and two ordered compact records in
`DOOM_FIRE_BUILD_PROBE`. The generator has no dimension arguments or fallback.
It produces exactly 150 distinct frames numbered 0 through 149 at exactly
160×96, using one textual static SQL MODEL operation with explicit
`RULES SEQUENTIAL ORDER` and rule dimension `ORDER BY frame ASC, y DESC, x ASC`.

The independent coordinate-only noise, source rule, wrapped lateral recurrence,
decay, byte order, and canonical RLE are frozen in the evaluator. All 2,304,000
cells, all 150 SHA-256 frame identities, the complete animation digest
`b1eac353252af51494cfe4ca77a80ac2bad502761bbaf79dd382f1146cb7e4ba`,
604,369 exact run rows, and small/full feasibility facts are independently
derived. A conservative 256 MiB pre-insert memory ceiling is fail-closed.

The live runner executes full generation twice, independently decodes and hashes
every Oracle frame, compares the complete ordered run/hash identity, and rejects
partial commits, reduced dimensions, redundant JSON/BLOB/cell persistence,
random/time/session seeds, dynamic SQL, evaluator coupling, and adaptive retry.

## Self-contained evidence

```text
PASS T9.1-EVAL-SELF-CHECK (8725437/8725437 fixture-reference assertions; 2304000 independent cells)
PASS T9.1-EVAL-MUTATION-SELF-CHECK (26/26 isolated mutations killed)
PASS T9.1-SOURCE-POLICY-SELF-CHECK (synthetic positive and negative canaries; production absent)
PASS T0.4 (8/8 assertions)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
PASS T9.1-INTEGRITY (14/14 candidate files and 3 approved baselines)
```

The manifest declares 21 stable IDs and 12,731,825 assertions. Oracle MODEL is
mandatory with no fallback; the policy and syntax follow Oracle's documented
MODEL multidimensional arrays, ordered computation, rule `ORDER BY`, and
`SEQUENTIAL ORDER` behavior.

## Visual and upstream blockers

`fixtures.json` deliberately records visual review as `PENDING`, with null
artifact identity and zero reviewed frame hashes. No PNG, animation, or invented
visual golden was created. After T8.1 and T8.2 acceptance, production must pass
the full 150×160×96 live runner. Only then may a real animation decoded from the
accepted Oracle RLE rows be inspected and the visual checkpoint separately
recorded. Any full-size failure blocks T9.1; reducing frames or dimensions is
forbidden.

Frozen manifest: `evaluator/integrity.pending-T9.1.json`.
Production interface file expected by the policy audit:
`sql/presentation/090_model_fire.sql`.
