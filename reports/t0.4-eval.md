# T0.4-EVAL task report

Status: **approved by the user on 2026-07-14**. The evaluator baseline is now
authorized for T0.4 implementation and P1 work.

## Route

`T0.4-EVAL | Terra | high | attempt 1`

The evaluator was authored in its evaluator-only context. The work is limited to
`evaluator/`, the generic mutation tooling, the T0.4 verifier entrypoint, and
this report/routing record. The two implementation examples live below
`evaluator/dummy/` and are test inputs, not production code.

## Delivered

- Test-only Playwright 1.61 container and a read-only Compose boundary with an
  internal ORDS-only network, read-only evaluator/held-back/production mounts,
  tmpfs scratch, dropped capabilities, and no Docker socket.
- Exact T0.4 test-id/assertion manifest plus SHA-256 integrity manifest marked
  `APPROVED BY USER 2026-07-14`; no update/golden-generation command exists.
- Static production-source audit and CLI for evaluator/golden/test leakage,
  test-environment branching, process inspection, disabled assertions, and
  unconditional success shortcuts.
- Confined hidden-seed JSON loader for a read-only external mount. Held-back
  values and expected outputs are not present in the repository.
- Isolated mutation runner, the full 20-mutation catalog, and positive killed /
  negative surviving canaries that distinguish semantic failure from build,
  deployment, health, timeout, and incidental failures.
- Deterministic Chromium configuration and guards for `.only`, skip/fixme,
  console/page/request failures, machine result IDs, project, retry count,
  statuses, and explicit assertion counts.
- Deliberate dummy good/bad production audit inputs and 13 adversarial harness
  attacks covering the Section 6.1 lifecycle requirements.

## Verification evidence

Commands run from the repository root:

```text
./verify.sh task T0.4
PASS T0.4 (8/8 assertions)

./verify.sh evaluator-self-test
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)

node tools/mutations/run.mjs
passed=true; canary-killed=true; canary-survives=true
```

The static production audit also reports green on the currently absent
production roots. The bad dummy is rejected for `expected-path-read`.

## Approval record

The user explicitly approved this T0.4 evaluator baseline on 2026-07-14 with
the statement: `i approve, get it all fixed up`. This approval clears the plan's
pre-P1 human gate. Future evaluator, fixture, golden, manifest, or reviewed-hash
changes still require renewed approval under the plan.

## Review boundary

The integrity hashes provide change detection, not a security guarantee. User
review should focus on the fixture intent, exact manifest counts, mount/network
boundary, audit false-positive risk, and mutation kill semantics.
