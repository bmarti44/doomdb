# T12.2 profile-guided optimization evaluator report

Status: **APPROVED AND FROZEN under user standing authorization 2026-07-15;
live acceptance is pending T12.1, T11.2, production implementation and real
local/cloud execution**.

Route: Sol high for SQL and Terra for transport/client changes. This evaluator
was authored independently without starting Oracle, reading unfinished
production, or editing shared routing, bootstrap, verification or production
files.

The frozen contract accepts only a complete profile-led attempt journal. Each
attempt repeats the exact Section 6.6 replay, records a nonempty allowed and
technically distinct change, targets the dominant measured stage, retains raw
evidence even on regression, preserves public decompressed schema and all golden
digests, and passes full correctness and mutation gates. Journal entries are
contiguous and hash-chained to make omitted or rewritten attempts detectable.

The stop rule requires the last two technically distinct, human-reviewed
attempts each to improve median end-to-end latency by less than five percent
against the best accepted state available before that attempt. Review explicitly
rejects intentionally ineffective work. The selected best revision must then
complete independent 300-frame local and cloud verification replays. Final
local/cloud FPS and p50/p95 are derived from raw external samples and must be
labelled direct measurements, never estimates or portable claims.

Self-contained evidence:

```text
PASS T12.2-EVAL-SELF-CHECK
PASS T12.2-EVAL-MUTATION-SELF-CHECK
PASS T12.2-SOURCE-AUDIT (production intentionally absent during evaluator authoring)
PASS T0.4
PASS T0.4-EVALUATOR-SELF-TEST
```

The manifest declares 15 stable IDs and 2,248 assertions. Missing ancestry,
production, any attempt/raw artifact, correctness or mutation proof, final cloud
evidence, atomic provenance, or any validation error exits nonzero and cannot
print PASS.

Evaluator-only correction (2026-07-15): the original frozen schema embedded a
machine-report SHA inside the JSON body required to hash to that same SHA, an
unsatisfiable cryptographic fixed point. The corrected schema keeps the digest as
external evidence metadata and hashes a finite report payload that excludes only
that digest field. Raw-byte SHA verification and semantic comparison of every
remaining report field are both retained. The positive self-check now validates
real in-memory artifact bytes, and a separately tampered correctness report is
rejected. No optimization, stop-rule, golden, source, or production contract was
changed.
