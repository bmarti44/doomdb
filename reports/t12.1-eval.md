# T12.1 baseline/cursor-hygiene evaluator report

Status: **APPROVED AND FROZEN under user standing authorization 2026-07-15;
live performance acceptance is pending implementation and a passing T11.2 gate**.

Route: Terra medium. This evaluator was authored independently without starting
Oracle, benchmarking, reading unfinished production, or editing shared routing,
bootstrap, verification or production files.

The frozen contract requires the exact Section 6.6 300-frame replay (30 warm,
270 measured), evaluator-owned external timing, per-frame payload identities and
sizes, derived p50/p95 statistics, effective FPS, database/ORDS/decode/R1/R2
measurements, `ALLSTATS LAST` plans with runtime rows, and redacted `V$SQL`
parse/execution deltas. Bound statement shapes and force-matching signatures must
be stable across four poses and five command classes. There is deliberately no
machine-independent speed threshold.

Samples, plans, cursor snapshots, payload ledger, replay identity and the report
are distinct safe-path artifacts. The validator rereads each declared artifact,
checks exact bytes and SHA-256, requires atomic provenance, and recursively
rejects endpoints, authorization, credentials, bind values and secret-bearing
keys. Renderer/database stage timers are expressly forbidden from response-body
keys.

Self-contained evidence:

```text
PASS T12.1-EVAL-SELF-CHECK (15/15 fixture-contract assertions)
PASS T12.1-EVAL-MUTATION-SELF-CHECK (24/24 isolated mutations killed)
PASS T12.1-SOURCE-AUDIT (production intentionally absent during evaluator authoring)
PASS T0.4 (8/8 assertions)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
```

The manifest declares 13 stable IDs and 864 assertions. Missing production,
partial raw data, absent Oracle observations, unreadable artifacts, synthetic
markers, or any validation error exits nonzero and cannot print PASS.

External blockers: final live T11.2 S3/Oracle browser acceptance, the not-yet-
reviewed `scripts/verify-performance-baseline.mjs`, live local/cloud stacks, and
real execution. Frozen manifest: `evaluator/integrity.pending-T12.1.json`.
