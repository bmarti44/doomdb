# T6.2-EVAL movement and collision candidate report

Status: **APPROVED AND FROZEN — user standing authorization 2026-07-14**

Route: Sol high. This was an evaluator-only work item under PLAN Sections 0,
2, 3, 5, 6, task T6.2, and Appendix D. It added no production SQL, modified no
existing evaluator, and did not edit the root verifier.

## Reviewed production contract

The candidate fixes one session-bound table SQL macro:

```sql
DOOM_PLAYER_MOVE(p_session VARCHAR2, p_delta_x NUMBER, p_delta_y NUMBER)
```

It returns exactly one row containing start/destination position, destination
sector, destination view offset and absolute eye height, contact count, and both
stable blocker ids/fractions. It reads constrained `PLAYERS.NOCLIP`; noclip is
not accepted from the caller. T6.2 adds and reads these relational constants:

- radius 16, height 56, step height 24, view offset 41;
- fixed maximum two contacts;
- Doom linedef blocking flag bit 1.

Candidates are conservative BLOCKMAP and/or Spatial results followed by exact
finite-segment swept-circle contact. Blocking combines one-sidedness, the flag,
and session-current portal intersection. Contact order is
`(fraction, linedef_id)`, remaining displacement slides on the line tangent,
and destination BSP sector owns floor z and eye height. The macro proposes one
set-based row for the tic transaction; it does not mutate or commit by itself.

## Independent oracle and coverage

The evaluator-only analytic capsule-sweep oracle imports no production parser,
SQL, geometry helper, or engine definition. Sixteen hand-authored scenarios
cover:

- head-on and oblique sliding contact;
- a stable two-contact corner;
- open portal, exact valid step, excessive step, low opening, and closed door;
- explicit blocking flag and current rather than static sector heights;
- translated geometry and candidate-order invariance;
- long-motion tunneling and finite endpoint contact;
- database-owned noclip with destination floor/sector/eye resolution;
- earliest-fraction priority and exact-fraction linedef-id ties.

The disposable live-Oracle path creates only a mini-map schema, concatenates the
unchanged production script into it, and runs ten real macro scenarios. The
loaded production-schema path separately checks the interface, reviewed config,
zero movement, and database-owned noclip. Compile errors, absent rows, or schema
errors fail closed.

## Manifests and anti-reward-hacking

- 22 stable visible IDs / 372 declared assertions.
- 18 isolated semantic mutations / 18 independent witnesses.
- Source audit requires session-bound relational inputs, dynamic openings,
  analytic sweep/endcaps, conservative candidates, stable ordering, tangent
  sliding, BSP destination, and a fixed two-contact result.
- Source audit rejects procedural collision loops, dynamic SQL, caller noclip,
  client coordinates, evaluator/report/golden reads, scenario labels, expected
  document hashes, and process/caller inspection.
- Candidate integrity: 27/27 checked paths.
- Candidate manifest SHA-256:
  `ce2fb59e604d2b5a4fc5178824ccfe9cc77479e4229430d77033b43246afa6e5`.

## Commands and results

```text
node evaluator/t6.2/self-check.mjs
PASS T6.2-EVAL-SELF-CHECK (80/80 fixture-contract assertions)

node evaluator/t6.2/mutation-self-check.mjs
PASS T6.2-EVAL-MUTATION-SELF-CHECK (18/18 isolated mutations killed)

node evaluator/run-foundation.mjs
PASS T0.4 (8/8 assertions)

node evaluator/self-test.mjs
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)

candidate integrity verification
PASS T6.2-CANDIDATE-INTEGRITY (27/27)
```

`source-audit.mjs`, the production-schema Oracle probe, and the mini-map Oracle
probe are intentionally not green before T6.2 implementation exists. After
approval, implementation must not modify any T6.2 evaluator file or this
candidate baseline.

## Approval promotion

The user's explicit standing authorization for all remaining evaluator
candidates promoted this reviewed candidate on 2026-07-14. The promotion
changed approval metadata only; evaluator logic, fixtures, expectations,
production code, and root verification routing remain unchanged. The frozen
candidate content hashes still pass 27/27, with 22 stable IDs, 372 declared
assertions, 16 scenarios, self-check 80/80, mutation witnesses 18/18, and the
foundation/adversarial checks green. Promoted manifest SHA-256:
`199b20a2207fb724e8131bf43512fbc195d33e397df04cd07933938b7d4c2251`.
