# T2.2-EVAL binary parser evaluator report

Status: **approved by the user on 2026-07-14**. The exact reviewed evaluator
candidate is authorized for T2.2-IMPL.

## Route and boundary

`T2.2-EVAL | Terra | high | attempt 1`

This task added only evaluator inputs, expectations, checks, mutation
specifications, this report, and the routing record. It added no parser or other
production code and did not edit `verify.sh`. The approved T0.4 baseline and
`evaluator/integrity.json` remain byte-for-byte unchanged.

## Candidate delivered

- A 24,541-byte synthetic PWAD assembled from visible literal fields, pinned by
  SHA-256 `c93fe89470786182ecf7483c2151a9a0514b04f5b945f013258038abfdd48d11`.
- All ten map inputs: THINGS, LINEDEFS, SIDEDEFS, VERTEXES, SEGS, SSECTORS,
  NODES, SECTORS, REJECT, and BLOCKMAP.
- The non-map parser surface: PLAYPAL, all 34 COLORMAP bands, PNAMES,
  TEXTURE1/TEXTURE2, an odd-width composite with negative/overlapping patch
  origins, flat namespace, sparse sprite patch, Doom sound, and MUS header.
- Explicit duplicate-name provenance/last-occurrence behavior and an E1M2
  sentinel proving map-marker confinement.
- Tall-post continuation, transparent gaps, opaque palette index zero, node
  child flags, BLOCKMAP framing, REJECT bit order, signed fields, and raw THING
  flag expectations.
- Fifteen malformed fixtures with exact public error codes, including directory
  and lump bounds, malformed fixed-record sizes/post streams, REJECT/BLOCKMAP,
  and six kinds of out-of-range references.
- Nineteen stable test IDs and 235 declared assertions, clean-process
  byte-identical JSON checks, and ten semantic mutation specifications mapped to
  named kill tests.

## Evaluator self-check evidence

```text
node evaluator/t2.2/self-check.mjs
PASS T2.2-EVAL-SELF-CHECK (17/17 fixture-contract assertions)
```

`run-visible.mjs` is intentionally not runnable to green yet: it fails closed
until the implementation item supplies `tools/wad/parse.mjs`. Its public CLI,
success document, and stable rejection contract are documented in the adjacent
README.

## Approval review points

Review the hand-recorded values, especially raw THING bit 0x10 naming, tall-post
row 255, BLOCKMAP list framing, REJECT bit order, duplicate resolution, exact
error taxonomy, and the mutation-to-test mapping. Approval authorizes these new
T2.2 evaluator files only; it does not modify or revoke the approved T0.4
baseline.

## Approval record

The user explicitly approved this T2.2 evaluator candidate on 2026-07-14 with
the statement: `approved, continue`. The approval metadata in
`evaluator/integrity.pending-T2.2.json` records that authorization without
changing any reviewed file or hash. The candidate-era approval label inside the
hashed `test-ids.json` remains unchanged so the approved candidate stays
byte-for-byte identical to the version the user reviewed.
