# T10.3 local end-to-end evaluator report

Status: **APPROVED AND FROZEN under user standing authorization 2026-07-15;
live two-bootstrap acceptance remains blocked by unfinished upstream production
and the absent T10.3 host orchestration implementation**.

Route: Terra high for independent evaluator authoring (the task implementation
remains PLAN-routed Terra medium). This work did not inspect or implement the
unfinished production client, REST package, orchestration driver, bootstrap,
root routing, or running stack.

## Frozen complete-local contract

Two executions must begin with distinct unpredictable project identities, no
preexisting project resources, fresh credentials, and fresh volumes. Startup,
health, every suite, browser execution, whole-gate duration, and teardown have
hard reviewed bounds. The live database remains capped at exactly two CPUs and
two GiB. Both projects and all volumes and secret files are removed on every
exit path.

All 24 visible P0-P10 suite families execute exactly once per fresh run from the
non-root, read-only, capability-free evaluator container. This inventory covers
all visible SQL, API, simulation, mutation and Playwright tests through T10.2
plus foundation/adversarial controls. A nonzero exit, signal, timeout, empty
output, incomplete assertion total, SKIP, NOT RUN, TODO, missing/duplicate suite,
retry, snapshot update, or unhealthy service fails closed.

The final local matrix requires all fourteen P10-relevant core rows GREEN. Cloud
and Performance remain explicitly later P11/P12 rows. Production byte artifacts
and the live Oracle catalog are independently audited. Each run must emit exactly
sixteen canonical correctness hash domains, and the deterministic suite-count,
capability, hash, artifact and schema ledgers must be byte-identical between the
two independent fresh-volume executions.

## Independent evidence

```text
PASS T10.3-EVAL-SELF-CHECK (21/21 fixture-contract assertions)
PASS T10.3-EVAL-MUTATION-SELF-CHECK (24/24 isolated mutations killed)
PASS T10.3-SOURCE-POLICY-SELF-CHECK (synthetic positive and negative canaries)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
```

The frozen manifest declares 16 stable IDs and 816 assertions. It pins the
approved T10.1 manifest
`87ed9f9af1239536ac0d0fc559ca9f0ede21259ca601920f575a3059406120d6`
and T10.2 manifest
`76674b880fad2197d2b8da01c415a76ccf8675cb5d459ce597f250b97d6f42d1`.
Live acceptance is intentionally not run by this evaluator-authoring item and
cannot pass until every upstream implementation and visible route is complete.
