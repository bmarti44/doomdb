# T3.3-EVAL BSP-location evaluator report

Status: **approved by the user on 2026-07-14**. The exact reviewed evaluator
candidate is authorized for T3.3 implementation.

## Route and boundary

`T3.3-EVAL | Sol | high | attempt 1`

This evaluator-authoring item added only the independent reference/oracles,
stable test IDs, semantic mutation specifications, source audit, behavior audit,
pending integrity metadata, this report, and its routing record. It did not add
or edit production SQL, schema, bootstrap order, or `verify.sh`, and it did not
modify any earlier approved evaluator artifact.

## Candidate delivered

- Fifteen stable visible IDs with 455 declared assertions and fifteen isolated
  semantic mutations.
- Fixed `DOOM_BSP_SIDE(...)` scalar and `DOOM_BSP_LOCATE(p_x,p_y)` table SQL
  macro interfaces. Location returns final subsector/sector, depth, and the
  observable root-to-leaf path signature, and must reuse the reviewed scalar
  predicate rather than carry a second implementation.
- Fourteen hand-authored side cases covering every axis sign branch, strict
  positive/negative non-axis crosses, origin equality, and collinear equality.
- A raw pinned-WAD oracle independent of production parser/seed output. It
  validates 681 nodes, 682 subsectors, player spawn subsector 115 / sector 140,
  and all 292 THINGS probes.
- A canonical all-THINGS document containing coordinates, leaf, sector, depth,
  and every branch. Its independently computed length is 27,158 bytes and its
  SHA-256 is frozen in evaluator-owned expectations.
- Root probes immediately below, exactly on, and immediately above the horizontal
  partition, plus far negative/positive coordinates to reject clamping.
- Fail-closed production source checks for `CONNECT BY`, last-node root,
  explicit axis handling, strict comparison, child-bit decoding, first-seg and
  facing-sidedef joins, deterministic ordering, bind references, and absence of
  recursion, loops, dynamic SQL, fixture literals, evaluator reads, or embedded
  expected hashes.
- A documented source-behavior reconciliation in `reports/bsp-audit.md` that
  distinguishes research into observable behavior from copying engine code.

## Evaluator self-check evidence

```text
node evaluator/t3.3/self-check.mjs
PASS T3.3-EVAL-SELF-CHECK (60/60 fixture-contract assertions)
```

The live Oracle runner intentionally remains red until an approved T3.3
implementation exists. It will invoke the macro through binds against the loaded
schema and will not accept compile failure, missing discovery, empty output, or
incidental errors as mutation kills.

## Approval review points

Please review the macro/result interface, path-signature format, hand cases,
all-THINGS hash strategy, boundary probes, facing-sidedef rule, mutation intent,
and production source restrictions. Approval authorizes only files hashed by
`evaluator/integrity.pending-T3.3.json`; all inherited approved baselines remain
immutable.

## Inherited T3.2 baseline refresh

The explicitly approved T3.2 Oracle-compatibility correction changed only the
inherited T3.2 integrity baseline. Pending T3.3 was refreshed to reference
`d617cdd9e5f8a36606d6606d6c61a514f46b3b5545f4526e48361a1c04208050`.
Its self-check's corresponding integrity pointer was updated; no T3.3 test
logic, fixture, expectation, production interface, or evaluator data changed.

The refreshed T3.3 candidate passed its unchanged 60/60 fixture-contract
self-check and all 14 candidate/inherited integrity hashes. Before approval,
the refreshed pending manifest SHA-256 was
`ebe56ce77538ca53261877d854835b1330c78e6e9bc06e9c5f54503bfd3a2424`.

## Approval record

The user explicitly approved this T3.3 evaluator candidate on 2026-07-14 with
the statement: `approved, continue`. Before promotion, all nine candidate file
hashes and all five inherited approved-baseline hashes were recomputed and
matched the frozen SHA-256 values. The evaluator checks passed exactly:

```text
PASS T3.3-EVAL-SELF-CHECK (60/60 fixture-contract assertions)
PASS T0.4 (8/8 assertions)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
PASS T3.3 integrity (14/14)
```

The approval metadata in `evaluator/integrity.pending-T3.3.json` records that
authorization without changing any reviewed evaluator file or candidate hash.
Candidate-era approval labels inside hashed files remain unchanged so the
approved candidate stays byte-for-byte identical to the version the user
reviewed. The approved manifest SHA-256 is
`6289853afe8d6966dfb46de6d06df8c68cd4f2a37a794b778df497bbd8b38a73`.

## Approved fractional-bind correction

The user explicitly approved the minimal evaluator-only correction on
2026-07-14. One `(792,64.25)` probe was added to the existing
`T33-BIND-SAFETY` path with the independently derived subsector 157, sector
147, depth 13, and exact root-to-leaf path. No production file or interface was
changed. The corrected evaluator passes 61/61 self-check assertions and
455/455 live assertions. `T33-M12-ROUND-BINDS` now fails specifically at the
fractional-bind assertion; the other fourteen mutation results are unchanged.

The corrected approved integrity manifest SHA-256 is
`8ccb54c64ed3e4e34ec3e1f84cda03a3b3ebe4a7ec8bf26c5688ab0b96260e37`.
