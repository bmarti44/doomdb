# T11.1 cloud database evaluator report

Status: **APPROVED AND FROZEN under user standing authorization 2026-07-15;
live acceptance is explicitly BLOCKED until real Autonomous Database credentials,
managed ORDS access, and the production cloud verification driver exist**.

Route: Terra high. This was independent evaluator-only work. It did not connect
to, inspect, create, alter, deploy, or destroy external cloud infrastructure and
did not edit production deployment, SQL, root routing, bootstrap, or secret files.

## Frozen live-cloud contract

The gate starts with byte-identical reviewed P0 capability and transport probes.
It rejects unsupported features, changed probe copies, local Oracle/ORDS
substitution, proxying, prerecorded responses, partial output, skips, dry runs,
and missing credentials. Deployment must use SQLcl `26.2.0.181.2110` and the
canonical ordered schema, seed, engine, and REST bytes.

Acceptance requires live Autonomous Database 23ai-or-later and managed ORDS
provenance; resource observations; least grants; exactly two reviewed AutoREST
objects and no custom modules/handlers; zero invalid objects, SQL errors, or
constraint drift; all 24 local/cloud seed domains equal by count and canonical
SHA-256; and all 17 direct API cases through the attested managed ORDS origin.

Credentials, endpoints, wallet material, target names and authorization values
are forbidden from retained evidence. Only content hashes and redacted live
observations may survive, and evidence is published atomically after success.

## Independent evaluator evidence

Self, mutation, source-policy, frozen-integrity, foundation and adversarial
results are recorded by the candidate scripts. The manifest declares 14 stable
IDs and 684 assertions with 24 focused mutations. Live execution was not and
cannot be run by this evaluator-authoring task without external authority.

```text
PASS T11.1-EVAL-SELF-CHECK (22/22 fixture-contract assertions)
PASS T11.1-EVAL-MUTATION-SELF-CHECK (24/24 isolated mutations killed)
PASS T11.1-SOURCE-POLICY-SELF-CHECK (synthetic positive and negative canaries)
PASS T11.1-EVAL-INTEGRITY (14/14 frozen files and baselines)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
```

Frozen manifest: `evaluator/integrity.pending-T11.1.json`, SHA-256
`04d1282dfc05f918a471737ccef1b065232c4d4ff7977abbf675dda3f03459ea`.

External blockers are therefore explicit: required `ADB_*` environment values,
wallet/connection access, a real target Autonomous Database, its managed ORDS
origin, and the not-yet-reviewed `scripts/verify-cloud-database.sh`. Credential
absence must yield nonzero `NOT RUN`, never PASS.
