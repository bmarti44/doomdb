# T8.2 evaluator report

Status: **APPROVED AND RE-FROZEN under user standing authorization 2026-07-15
after accepted T7.1-T7.3 dependency refresh; T8.1 live visual acceptance remains
pending**.

Route: Terra high. This was evaluator-only work. It added no production code,
did not inspect unfinished workflow implementation, and did not modify bootstrap,
drop, root verification, or routing files.

## Frozen interface and behavior

The public package remains exactly `DOOM_API.NEW_GAME`, `STEP`, `SAVE_GAME`,
`LOAD_GAME`, `START_REPLAY`, `STEP_REPLAY`, and `GET_ASSET`. Every control-state
change is an exact version-one STEP command. Menus use `OPEN`, `DOWN`, `UP`,
`SELECT`, `BACK`, and terminal `RESTART`. Required cheats are `GOD`, `ALL`,
`NOCLIP`, and `FULLMAP`.

The evaluator resolves the charter's otherwise unaddressed public rewind seam
without adding an endpoint: `cheat:"REWIND:<tic>"` is a reserved STEP workflow
encoding with canonical unsigned decimal. It creates a verified continuation
lineage at the requested logical tic while preserving a globally monotonic
command sequence and the old replayable lineage.

Direct HTTP and Playwright scenarios bind exact pause/menu/automap transitions,
all four cheat effects, save/load and rewind continuation equality, independent
replay, death/restart, intermission, atomic errors, request paths, response keys,
RLE coverage, frame hashes, PLAYPAL mapping, and raw 320x200 canvas RGBA. Death
and intermission browser sessions must be evaluator-provisioned; missing tokens
fail rather than skip. No screenshots or invented visual goldens were added.

## Independent evidence

```text
PASS T8.2-EVAL-SELF-CHECK (36/36 fixture-contract assertions)
PASS T8.2-EVAL-MUTATION-SELF-CHECK (24/24 isolated mutations killed)
PASS T8.2-SOURCE-POLICY-SELF-CHECK (synthetic positive and negative canaries; production audit deferred)
PASS Playwright discovery (2/2 exact Chromium ids; no skip/fixme/only)
PASS T0.4 (8/8 assertions)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
PASS T8.2-INTEGRITY (19/19 candidate and inherited entries)
```

The frozen manifest declares 28 stable IDs and 323,056 assertions. Its browser
submanifest declares 2 IDs and 164 assertions, included within that total. The
24 mutations cover every control, persistence, terminal, canvas, network,
ordering, atomicity, and anti-coupling defect class.

## Dependency refresh and handoff

T7.1, T7.2, and T7.3 production are accepted and their final evaluator identities
are pinned. T8.1 still requires its real route and visual checkpoint. After that
passes, provision deterministic dead and intermission sessions from evaluator-
owned setup, set the documented endpoint/session environment, and run
`evaluator/t8.2/run-visible.sh`.
Production source, browser network, direct API, both Chromium scenarios, all
28 IDs / 323,056 assertions, and all 24 mutation witnesses must pass together.

Refreshed inherited identities are T7.1 `45be1db5...`, T7.2 `764dda2f...`,
T7.3 `6267d2dc...`, and T8.1 `9d397f03...`. The re-promoted T8.2 evaluator
manifest is `cd36b384dc61111914f9e6c05289cff1b02f3c4d73b644b2705a05f8e1ddb791`.
