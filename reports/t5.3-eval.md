# T5.3 masked textures and world sprites evaluator candidate

Status: **APPROVED AND RE-FROZEN — standing user authorization 2026-07-15**.
No production SQL, evaluator behavior, root verifier, or upstream render contract
was modified.

Route: `T5.3-EVAL | Sol | high | attempt 1 | masked/sprite projection and occlusion oracle | candidate green`.

The fixed interface is two bound table SQL macros:
`DOOM_R2_MASKED_CANDIDATES(p_session VARCHAR2)` and
`DOOM_R2_MASKED_PIXELS(p_session VARCHAR2)`. The candidate macro makes every
opaque masked-middle or world-sprite sample and each screen, sector, and wall
clip fact observable. The pixel macro returns only deterministic winners, at
most once per screen pixel. The complete column contract is frozen in the
evaluator README.

The independent JavaScript oracle covers transparent two-sided middle textures,
billboard projection, all eight viewer-relative rotation bins, dual-name lump
mirroring, transparent versus palette-zero sampling, screen/sector/wall clips,
nearer sprites, and equal-depth ordering. Winner order is `(depth, source class,
source id, asset y, asset x)`, with masked textures before sprites at equal
depth. Joint camera/object translation is invariant.

Five mini-scenes have exact candidate counts, pixel documents, and SHA-256s.
The pinned WAD is also decoded independently for spawn and two diagnostic poses;
visible center-line object identities, types, depth, columns, rotations, spot
rows, and canonical hashes are frozen. These diagnostics contain respectively
20, 1, and 1 independently visible world-object observations.

The closure audit confirms 44 renderable E1M1 thing types, 66 non-weapon world
states, and 123 seeded sprite patches. Every declared state rotation resolves.
The production contract additionally requires explicit projectile and effect
catalog classifications for objects spawned by engine actions; these may not be
silently substituted with weapon overlay frames.

The manifest contains 17 stable IDs and 988 declared assertions. Eighteen
isolated mutations cover holes, rotations, mirrors, every clip stage, wall-depth
equality, nearer and equal-depth order, missing closure rows, projectile class,
early rounding, procedural loops, immutable map-thing rendering, omitted sidedef
offsets, and embedded diagnostics. The fail-closed source audit also rejects
dynamic SQL, recursive render CTEs, early depth rounding, evaluator reads, and
fixture/hash coupling.

Candidate evidence:

```text
PASS T5.3-EVAL-SELF-CHECK (346/346 fixture-contract assertions)
PASS T5.3-EVAL-MUTATION-SELF-CHECK (18/18 isolated mutations killed)
PASS T0.4 (8/8 assertions)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
PASS T5.3-EVAL-INTEGRITY (15/15 candidate and inherited entries)
PASS T5.3-EVAL-TRANSITIVE-INTEGRITY (206 checked entries)
PASS T5.3-SOURCE-AUDIT (1 SQL files; set-based masked and sprite composition)
PASS T5.3-VISIBLE-GOLDEN (3/3 human-reviewed database PNGs; 7/7 frozen artifacts)
PASS T5.3-ORACLE-PRODUCTION
```

The production source policy and live Oracle acceptance are green. Foundation
and all 13 adversarial evaluator self-tests are green.

## Dependency-only refresh and promotion

The accepted evaluator pins final T5.1 manifest
`7b02f8ce55c7f67493134cae9c2bc928340fe39b5abfc5457a3dda4c0e5844f7`,
final accepted T5.2 manifest
`b95895c22b3fb8cf5df42a314538ecdc2802abb744815196604070fab900d13c`,
and human-reviewed actual T5.2 visible integrity
`e59b635cdda850ae092d940ac38fb62cbf8e8b2e1ea6812ad67b61a7ebcc4995`.
Only inherited pointers, matching self-check guards, approval metadata, and this
report changed. At that dependency-only checkpoint, T5.3 still had no accepted
visual golden; the later actual-output review below closes that checkpoint.

Frozen evaluator manifest SHA-256:
`8e5969b517dac26fa2143dfd6cbedee9cde1ec1a0e09eca775a4f72070aebc1b`.

The two frozen SQL-macro interfaces remain
`DOOM_R2_MASKED_CANDIDATES(p_session VARCHAR2)` and
`DOOM_R2_MASKED_PIXELS(p_session VARCHAR2)`.

## Source-audit correction cascade refresh

After T5.1's evaluator-only source-audit correction, this second dependency-only
promotion replaced only the final T5.1 and cascaded T5.2 manifest pointers, their
matching self-check literals, and report/integrity metadata. The reviewed T5.2
visible integrity remains byte-identical. No T5.3 behavior, fixture, expectation,
test ID, assertion, mutation, reference, Oracle runner, source policy, production
source, or golden changed. Visual goldens were still pending at that historical
refresh and are accepted only by the independent review below.

## Oracle schema-drift correction

The evaluator's original positional `SECTOR_STATE` insert supplied seven values.
After T6.3 added `DAMAGE_CLOCK`, that statement fails against the final eight-column
schema with `ORA-00947` before any masked-render assertion can execute. The narrow
correction names the seven evaluator-owned columns explicitly and supplies
`DAMAGE_CLOCK=0`; the independently managed `LIGHT_TIMER` column keeps its schema
default. A self-check now freezes both the explicit column list and value mapping.

This correction changes only `oracle-production.sql`, its two metadata guards,
and evaluator approval/report integrity. It does not change masked or sprite
behavior, fixtures, expectations, 17 IDs/988 assertions, 18 mutation witnesses,
the source policy, production SQL, interfaces, or goldens. A live Oracle compile
probe printed `PASS T5.3-SECTOR-STATE-LIVE-SYNTAX`; self 346/346, mutations 18/18,
source audit, 14/14 direct and 198-entry transitive integrity, foundation 8/8,
and all 13 adversarial evaluator tests remain green. Visual goldens remained
pending at that correction checkpoint.

## Independent actual-output visual approval

Three fresh 320x200 database frames were captured and then recaptured from the
preserved isolated stack. The repeated SQL identities, diagnostics, overlay
source sets, and overlay counts matched exactly. A from-scratch decoder verified
the PNG signature, every chunk CRC, concatenated IDAT stream, all PNG filters,
the complete 256-entry palette, and exactly 64,000 pixels per frame in canonical
column-row order.

- Spawn east: frame
  `f34741d9374aa2f89eb172ed87e76e027285506d6589b61f3c77514ec0c96b81`,
  PNG `3b9dec0139697c497c5d9dbfb79a0c7292915f540ca8c44480a47a1f85a25a3b`,
  7,106 overlay pixels. Six `AQMETL15` masked grates retain transparent openings
  into the room while their opaque bars correctly cover portions of layered
  corpses, monsters, and pickups. The visible `POSSA8`, `POSSA1`, `POSSA4`, and
  `TROOA1` variants provide concrete rotation evidence; near corpse/sprite layers
  cover farther content without leaking through walls.
- Diagnostic north: frame
  `5ae974c2bd0fd0b398dbdb995609e469a973295457d82f3e0826048387ac22d9`,
  PNG `0549391827e1086f0781cb26bd501f169841136371bfec60742a6b6c6d54effd`,
  335 overlay pixels from only `SPRITE:58:POSSL0`. The centered sprite is cleanly
  clipped behind the foreground platform with its upper pixels still visible.
- Diagnostic south: frame
  `3fe12e7333d21705c7227e28ca1c6e22cc5acb72c7d6819850c24985c7801c9a`,
  PNG `49892ad4766f69c967f7dd15962480102c688785d7b349931f096b8c6ca3dfd1`,
  105 overlay pixels from only `SPRITE:239:PLAYW0`. The small player sprite is
  appropriately hidden by the stepped foreground geometry rather than drawn on
  top of it.

All three original-resolution images have coherent perspective and palette use,
correct foreground/background layering, and no transpose, tearing, bands,
unexplained holes, palette corruption, or edge wrap. The reviewed artifact
manifest is `goldens/integrity-T5.3.json` at
`24b543562a13c80db22edd376e93a7a5b3c30d28dc2e01db7d28a165210c0860`.
This approval freezes only the seven actual-output artifacts. Evaluator behavior,
17 IDs/988 assertions, 18 mutation witnesses, Oracle semantics, production SQL,
and both macro interfaces are unchanged.
