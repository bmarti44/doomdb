# T6.3 evaluator report

Status: **APPROVED AND FROZEN — user standing authorization 2026-07-14**

Route: Sol high. This was evaluator-only work for PLAN T6.3. It added no
production SQL, did not inspect or coordinate a future implementation, and did
not modify root verification or shared bootstrap routing.

## Frozen contract

The internal session-bound interface is `DOOM_WORLD_MACHINES.ADVANCE(session,
tic, previous_x, previous_y)`. It runs inside T6.1's transaction, reads the
database command/current player/map geometry, and owns no commit. A caller does
not identify a line, sector, key, range result, side, or crossing result.

The independent oracle covers all E1M1 line specials 1/2/11/23/26/62/88/117
and sector specials 1/7/9/12. Reviewed fixed definitions are use range 64,
button 35 tics, normal/blazing door speeds 2/8, door wait 150, lift speed/wait
1/105, damage 5 every 32 exposure tics, and synchronized strobe 5 bright/35
dark. Random blinking consumes the relational Doom random table.

Coverage includes focused and combined replays, range and side denial, exact
crossing direction, once/repeat behavior, blue-key denial without consumption,
tagged targets, button restoration, door reversal, lift occupancy, damage
cadence, secret once, both light machines, stable event ordinals, and exit
completion. Source audit rejects evaluator coupling, embedded fixture IDs,
caller-authored activation, dynamic SQL, and transaction ownership.

## Independent evidence

```text
PASS T6.3-EVAL-SELF-CHECK (67/67 fixture-contract assertions)
PASS T6.3-EVAL-MUTATION-SELF-CHECK (20/20 isolated mutations killed)
PASS T0.4 (8/8 assertions)
PASS T0.4-EVALUATOR-SELF-TEST (13/13 attacks rejected)
```

The manifest declares 28 stable IDs and 906 assertions. Production source audit
and the loaded Oracle probe intentionally remain red until T6.3 production is
implemented. The candidate manifest binds 16 prior integrity baselines and 10
T6.3 files. It was promoted under the user's standing authorization without
changing evaluator behavior or root routing.

## Evaluator-only correction

The first frozen source policy accidentally matched the suffix `P_LINEDEF` in
the mandatory relation name `DOOM_MAP_LINEDEF` (and likewise `P_SECTOR` in
`DOOM_MAP_SECTOR`). The caller-authored-input ban now requires an identifier
word boundary before `P_` or `CALLER_`. Direct inputs such as `P_LINEDEF` and
`CALLER_SECTOR` remain forbidden, while the required map relations are allowed.
Two internal policy assertions pin both sides of that boundary. No fixture,
reference behavior, test ID, assertion count, mutation contract, production
interface, or Oracle expectation changed. The corrected evaluator was
automatically re-frozen under the user's standing authorization after 67/67
self-check assertions, 20/20 mutation kills, source audit, foundation 8/8,
adversarial 13/13, and all 26 integrity entries passed.

Corrected source SHA-256:
`ba2949d252a637ffb73a25cf887ce082b7a98a3525bcfd36ed8f4acd0f215dd8`.
Re-frozen manifest SHA-256:
`3e4a5d765cd55dc06902f04fc5076a5c5bd9ff2d56a9df0cf6ad2dd17ee2ee23`.

### Oracle legality correction

The Oracle runner also called SQL-only `STANDARD_HASH` directly as a PL/SQL
expression, which a live Oracle probe rejected with `PLS-00201`. The runner now
selects the identical `lower(substr(standard_hash(..., 'MD5'), 1, 32))`
expression from `dual` into the same token variable. The hash input, algorithm,
truncation, casing, and resulting token bytes are unchanged. A live corrected
syntax probe completed and the full runner parsed beyond this statement before
stopping at production prerequisites not installed in that stack. No fixture,
reference behavior, test ID, assertion count, mutation, interface, or production
file changed. The concurrent approved T4.3 baseline refresh was also rebound.

Final gates passed: evaluator self-check 67/67, mutations 20/20, source audit,
integrity 26/26, foundation 8/8, and adversarial 13/13. Corrected Oracle-runner
SHA-256:
`efd30d78b7d45bb7e0e908e81a5158692af55e639da6512062f6aad11fad7b4c`.
Re-frozen manifest SHA-256:
`29a7574575f2fb765d7d4179447d9fb37d465cca823aed7c022f8b74790bfb83`.

## Implementation handoff

Implement only `sql/sim/030_world_machines.sql` plus reviewed relational config,
schema/install wiring needed by that source. Integrate `ADVANCE` into Appendix F
after movement and before later actor stages. Do not modify any file under
`evaluator/t6.3/` or this frozen manifest. Run `bash evaluator/t6.3/run-visible.sh`
and prove all 28 IDs / 906 assertions plus all 20 mutation witnesses. Preserve
T6.1 transaction ownership and T6.2 movement behavior.
